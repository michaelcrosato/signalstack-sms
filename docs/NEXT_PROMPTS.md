# Next Prompts

This file is the handoff for the next automated loop. It should stay short enough that a new agent can understand the next work quickly.

## Read First

1. `docs/CANONICAL_IMPLEMENTATION_PLAN.md`
2. `PLAN.md`
3. `docs/CURRENT_STATE_MATRIX.md`
4. `planning/CONSENSUS-2026-05-21.md`
5. `AGENTS.md`
6. `contracts/**`
7. `docs/LOCAL_GATE.md`

## Current State

- Latest run: Run 661 added live-worker deployment-class coverage proving object-shaped deployment-class values with hidden own string or symbol metadata deny without accessor metadata being read, callable metadata being invoked, object-valued metadata being coerced, or supplied controls being inspected while the still-reserved `production-live-campaign` class remains unsupported.
- The backend foundation is strong: tenant helpers, contacts, campaigns, queue jobs, inbox, compliance gates, fake AI, local billing/analytics, provider metadata, Twilio webhook foundations, readiness audit, operations inventory, and validation gates.
- The settings operations pages use compact related-link headers; keep those surfaces read-only and do not expand operations UI unless it directly supports release safety or product work.
- The browser product now has an initial `/dashboard` shell, `/dashboard/contacts` list/import workflow with archived restore links, `/dashboard/contacts/:contactId` local detail/edit/archive/restore/merge workflow, `/dashboard/campaigns` compose/preflight/local schedule workflow, `/dashboard/campaigns/:campaignId` local draft edit and queued cancel workflow, `/dashboard/inbox` local thread workflow, `/dashboard/templates` local template create/list workflow, `/dashboard/templates/:templateId` local detail/edit workflow, `/dashboard/analytics` local overview detail, and `/dashboard/compliance` local readiness detail.
- The only live external-impact route is the isolated gated live-test SMS demo path, and `/settings/api` plus API operations unit coverage now mark that route row as external impact with Twilio allowlist-gate safety copy while keeping every other listed route no-external-impact.
- API operations implemented-route reverse coverage now reuses the shared contract route-method extractor, so named-export, typed const, and non-code-masked route method detection, including escaped-regex and character-class regex-literal masking, stays aligned with `npm run contracts:check`.
- Mutating API authorization coverage now includes optional-bracket `Object`/`Reflect` descriptor/prototype helper calls, direct request/request-alias body-reader property aliases, non-null local `globalThis` aliases, local aliases assigned from `globalThis.Object`/`globalThis.Reflect`, assigned and whole-parenthesized type-asserted or `satisfies` direct `Request` constructor and `Request.prototype` aliases, type-asserted destructured `globalThis` `Object`/`Reflect`/`Request` aliases, parenthesized plus whole-parenthesized type-asserted `globalThis` root `Request`/`Reflect` member aliases, and parenthesized `satisfies` local/root `globalThis` `Reflect` aliases before `requireApiRole`, while preserving safe after-gate controls.
- Twilio webhook validation rejects non-string form parts and duplicate form field names before signature validation and signs all string form parameters including unknown provider fields. Normalization trims inbound `From`/`To` addresses before local contact/message creation, rejects whitespace-only inbound bodies without trimming stored nonblank body text, trims provider message IDs and error codes, falls back from blank modern Twilio message/status fields to nonblank legacy aliases, normalizes delivery-status casing/whitespace before local idempotency keys are derived, and clears stale opposite terminal timestamps for delivered versus failed/undelivered transitions, so retry formatting drift and local delivery metadata do not create confusing duplicate or stale states. `/settings/webhooks` now consumes frozen operation metadata rather than inline route copy.
- Campaign and template create/update route coverage now proves malformed JSON returns `400` before local repository mutations run. Contact create/update/merge/import route coverage proves malformed JSON returns `400` before local contact upsert/update/merge, CSV parsing, or import repository mutations run. Inbox/demo inbound route coverage proves malformed JSON returns `400` before local inbound message, conversation message, assignment, note, or resolve repository mutations run. Fake AI route coverage proves malformed JSON returns `400` before conversation lookup, fake provider execution, or local `AI_REQUEST` usage metering can run. Billing usage, campaign preflight, compliance settings, provider number metadata, provider settings, and live-test SMS route coverage proves malformed JSON returns `400` before local usage records, local preflight repository work, compliance updates, readiness audit writes, provider metadata writes, provider settings rendering, or gated live-test send helpers can run. Readiness audit and provider credential rotation route coverage proves unsupported JSON/CSV export filters return `400` before local reads or CSV serialization and supported filters stay bounded to local tenant metadata. Compliance settings route coverage proves role denials return before request-body parsing, and successful admin requests update only local compliance profile metadata and local readiness audit records before evaluating the local messaging hard gate without provider metadata writes or live-test SMS helpers. Provider number metadata route coverage proves role denials return before request-body parsing, and successful admin requests upsert only local provider number metadata without provider settings rendering, provider credential persistence, or live-test send helpers. Provider settings delete route coverage proves role denials return before local credential metadata is cleared, and successful deletion clears only local metadata before rendering secret-safe provider settings. Campaign schedule route coverage proves role denials return before request-body parsing, malformed or schema-invalid JSON returns `400` before local queue scheduling or BullMQ enqueue helpers can run, missing campaigns return `404` without BullMQ enqueue, and valid schedules persist a local queue job before optional BullMQ enqueue. Campaign cancel route coverage proves role denials return before local cancellation, missing campaigns return `404`, existing non-scheduled campaigns return `409` without queue or campaign mutations, and successful cancellation returns the locally paused campaign without provider, worker, BullMQ, billing, notification, SMS, or live-feature side effects.
- Local database and BullMQ workers now block every production-like runtime marker (`NODE_ENV`, `VERCEL_ENV`, `DEPLOYMENT_ENV`, and `APP_ENV`) even with dummy/demo-safe provider defaults.
- `docs/PRODUCTION_WORKER_POLICY.md` now defines the future production worker planning gate and `lib/queue/live-worker-controls.ts` pins the reserved `production-live-campaign` control checklist as frozen metadata with a supported status vocabulary, frozen data descriptor requirement, frozen plain supplied-control array requirement, enumerable indexed-data-slot exact-key-order evidence guard, tampered-prototype-array denial before indexed evidence inspection, inherited data-backed/inherited accessor/accessor/writable/configurable/non-enumerable/proxy-invalid indexed-slot denial, explicit hidden string/symbol control-array metadata denial, own control-array `constructor`, `toLocaleString`, `Symbol.toStringTag`, and `Symbol.asyncIterator` metadata denial without getter reads or data-backed invocation, own data-backed control-array coercion metadata denial without invocation, own object-valued control-array tag, iterator, async-iterator, well-known symbol, method-name, coercion, `constructor`, and `toLocaleString` metadata denial without coercion, own accessor-backed and data-backed well-known symbol control-array metadata denial without getter reads or invocation, own accessor-backed array method-name metadata denial without getter reads, own data-backed array method-name metadata denial without invocation, custom iterator control-array metadata denial without iterator invocation, own data-backed iterator and async-iterator metadata denial without invocation, hidden accessor/data-backed control-entry metadata denial without getter reads or callable invocation, own accessor-backed control-entry coercion metadata denial without getter reads, own data-backed control-entry coercion metadata denial without invocation, inherited data-backed occupied index-slot metadata, inherited data-backed extra index-slot metadata, non-index metadata, array iterator, async-iterator, `Symbol.toStringTag`, `toLocaleString`, well-known symbol, iteration-method, lookup-method, every-method, transform/reducer-method, mutator/visitor-method, copy-helper-method, Object-helper, legacy accessor-helper, prototype-accessor, and coercion invocation independence for exact frozen evidence, inherited array indexed-accessor, non-index metadata, constructor, `toLocaleString`, iteration-method, lookup-method, every-method, transform/reducer-method, mutator/visitor-method, copy-helper-method, Object-helper, legacy accessor-helper, prototype-accessor, unscopables, concat-spreadable, string-method symbol, iterator, async-iterator, `Symbol.toStringTag`, and coercion metadata independence for exact frozen evidence, inherited `Object.prototype[Symbol.iterator]` iterator independence for scratch-array evidence, inherited `Object.prototype.toLocaleString` and arbitrary inherited `Object.prototype` non-public metadata independence for exact frozen evidence, inherited `Object.prototype.hasOwnProperty` ownership-helper independence for exact frozen control-array density evidence, inherited `Object.prototype.propertyIsEnumerable` enumerability-helper independence for exact frozen descriptor-enumerability evidence, inherited `Object.prototype.isPrototypeOf` prototype-helper independence for exact frozen evidence, inherited `Object.prototype.constructor` metadata independence for exact frozen evidence, inherited `Object.prototype` legacy accessor-helper metadata independence for exact frozen evidence, inherited `Object.prototype` authorization-wrapper accessor/data-backed-public-field/coercion/tag and control-entry accessor/data-backed-public-field/coercion/tag metadata independence for exact frozen evidence, data-backed inherited `Object.prototype` coercion invocation independence for exact frozen live-worker evidence, malformed primitive control-entry denial, non-ordinary array/date/function-shaped control-entry denial, sealed-but-writable, configurable-public-field, hidden required public-field, hidden-string-control-entry and hidden-symbol-control-entry metadata denial with explicit authorization-path coverage, symbol-keyed control-entry public-field impersonator denial, duplicate-key, nullish public-field-value denial, proxy-invalid public-field-descriptor, malformed primitive public-field-value denial including malformed primitive status values, non-primitive public-field-value control-entry denial without coercion including boxed string `id`, `status`, or `requirement` values, and exact public-string matching for supplied control `id`, `status`, and `requirement` values without trimming or case normalization, missing/accessor/writable native length descriptor denial, nullish length-descriptor denial before indexed control reads, proxy-invalid configurable/enumerable length-descriptor denial without descriptor invariant leaks, exact checklist-size length descriptor requirement, mismatched, oversized safe-integer, boxed numeric, and non-coercive malformed primitive including boolean, bigint, symbol, string, and non-safe numeric length-descriptor denial before indexed control reads, hostile length-descriptor denial before indexed control reads, duplicate proxy-key denial on control arrays and authorization wrappers, ordinary-object entry requirement, own-enumerable-data-field public authorization guard, getter-safe identity/status checks, descriptor-valid proxy-wrapped exact evidence evaluation without `get` trap execution, proxy-trap-safe malformed-evidence denial including frozen-state traps, exact deployment-class string matching without trimming or case normalization, unsupported/malformed-class primitive evidence including bigint plus object-shaped impostor and inherited-coercion-hook short-circuit coverage before supplied controls are inspected, blank or whitespace-only deployment-class denial before supplied controls are inspected, exact-order own-enumerable frozen-data-field authorization-wrapper denial, authorization-wrapper sealed-but-writable denial before control inspection, authorization-wrapper hidden accessor/data-backed metadata denial before control inspection without getter reads or callable invocation, authorization-wrapper accessor-backed own coercion metadata denial before control inspection without getter reads, authorization-wrapper full and mixed symbol-keyed public-field impersonator denial before control inspection, non-enumerable wrapper public-field denial before control inspection, non-ordinary built-in object-shaped wrapper denial including maps, sets, weak collections, typed arrays, data views, promises, boxed primitives including boxed `Symbol` and boxed `BigInt`, regular expressions, errors, Web-platform records, weak references, finalization registries, and proxy-backed built-in wrappers before control inspection, non-ordinary array/date/function-shaped and tampered-prototype wrapper denial before control inspection, malformed wrapper-key and wrapper-shape denial before control inspection including hidden string/symbol wrapper metadata, extensible-wrapper denial, writable/configurable/proxy-invalid wrapper-field denial across both public wrapper fields including the `controls` descriptor, malformed primitive, function-shaped, and object-shaped non-array controls-evidence denial including weak collections, data views, boxed primitives including boxed `Symbol` and boxed `BigInt`, dates, regular expressions, errors, URLs, URLSearchParams, Web-platform records, weak references, finalization registries, proxy-backed maps, proxy-backed sets, proxy-backed weak collections, proxy-backed Web-platform records, proxy-backed array buffers, proxy-backed shared array buffers, proxy-backed or revoked proxy non-array records, proxy-backed typed arrays, proxy-backed data views, proxy-backed promises, proxy-backed boxed primitives including boxed `Symbol` and boxed `BigInt`, proxy-backed dates, proxy-backed regular expressions, proxy-backed errors, proxy-backed URLs, proxy-backed URLSearchParams, proxy-backed weak references, proxy-backed finalization registries, revoked proxy-backed built-in records including boxed `Symbol` and boxed `BigInt` and Web-platform records, proxy-backed or revoked proxy-backed array-prototype impostors without coercion, fallback, callable invocation, or object-trap inspection, hostile array-like, iterable object, and own or inherited `Symbol.toStringTag` array-impostor controls-evidence denial without getter reads or iterator invocation, inherited `Symbol.toStringTag` authorization-wrapper metadata denial before hostile control inspection, and nullish-control-evidence denial. Public worker readiness accepts runtime-unknown safety input, denies malformed deployment-class values before provider fallthrough, and fails closed for runtime-unknown or malformed `LIVE_MESSAGING_ENABLED` values. `npm run production-worker:check` keeps the current local/demo-only worker boundary present in docs/source/tests. `WORKER_DEPLOYMENT_CLASS` may be unset or `local-demo` only; it does not authorize live worker execution.
- Live campaign sending, live billing, live AI, production auth, production secrets, and production deployment remain blocked.
- Exact frozen live-worker evidence now also stays independent from inherited `Object.prototype.toLocaleString` metadata and arbitrary inherited `Object.prototype` non-public string or symbol metadata before the reserved live-worker class can authorize.
- Exact frozen control-array evidence now also stays independent from inherited `Array.prototype` Object-helper metadata (`hasOwnProperty`, `propertyIsEnumerable`, and `isPrototypeOf`), legacy accessor-helper metadata (`__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, and `__lookupSetter__`), and prototype-accessor metadata (`__proto__`) before the reserved live-worker class can authorize.
- Hidden control-array metadata with accessor-backed getters or data-backed callable values now denies without reading or invoking it before the reserved live-worker class can authorize.
- Own accessor-backed or data-backed `toLocaleString` metadata on otherwise valid supplied control arrays now denies without being read or invoked before the reserved live-worker class can authorize.
- Own accessor-backed or data-backed `constructor` metadata on otherwise valid supplied control arrays now denies without being read or invoked before the reserved live-worker class can authorize.
- Own object-valued `Symbol.toPrimitive`, `toString`, `valueOf`, `constructor`, or `toLocaleString` metadata on otherwise valid supplied control arrays now denies without coercing `Symbol.toPrimitive`, `toString`, or `valueOf` hooks before the reserved live-worker class can authorize.
- Own object-valued `Symbol.toStringTag`, iterator, async-iterator, well-known symbol, and array method-name metadata on otherwise valid supplied control arrays now denies without coercing hostile `Symbol.toPrimitive`, `toString`, or `valueOf` hooks before the reserved live-worker class can authorize.
- Own object-valued control-entry `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` metadata now denies without coercing hostile hooks before the reserved live-worker class can authorize.
- Own object-valued authorization-wrapper `constructor` and `toLocaleString` metadata now denies without coercing hostile hooks or inspecting supplied controls before the reserved live-worker class can authorize.
- Own object-valued authorization-wrapper `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` metadata denies without coercing hostile hooks or inspecting supplied controls before the reserved live-worker class can authorize.
- Own object-valued authorization-wrapper iterator, well-known symbol, Object-helper, legacy accessor-helper, and `__proto__` metadata now denies without coercing hostile hooks or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with own data-backed callable tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, `constructor`, `toLocaleString`, or coercion metadata now deny without invoking callable metadata or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with inherited data-backed callable tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, `constructor`, `toLocaleString`, or coercion metadata now deny without invoking callable metadata or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with own object-valued tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, `constructor`, `toLocaleString`, or coercion metadata now deny without coercing hostile hooks or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with inherited object-valued tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, `constructor`, `toLocaleString`, or coercion metadata now deny without coercing hostile hooks or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with inherited accessor-backed tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, `constructor`, `toLocaleString`, or coercion metadata now deny without reading getters or inspecting supplied controls before the reserved live-worker class can authorize.
- Object-shaped deployment-class values with hidden own string or symbol metadata now deny without reading accessor metadata, invoking callable metadata, coercing object-valued metadata, or inspecting supplied controls before the reserved live-worker class can authorize.
- Seeded product-demo stability was rechecked on 2026-05-24 with 10 Chromium tests covering dashboard, contacts, contact detail/archive/restore/merge, campaign create/preflight/schedule, campaign detail edit/cancel, inbox, templates, analytics, and compliance.
- Exact deployment-class string coverage includes leading spaces, trailing spaces, tabs, newlines, carriage returns, CRLF pairs, vertical tabs, form feeds, invisible Unicode escape padding, and Unicode line/paragraph separator padding before supplied live-worker controls can be inspected.
- Built-in object-shaped deployment-class impostor coverage now includes maps, sets, weak collections, typed arrays, data views, promises, regular expressions, errors, runtime-supported Web-platform records, runtime-supported WebAssembly records, runtime-supported Web Crypto records, proxy-backed and revoked proxy-backed runtime-supported platform records, proxy-backed or revoked proxy object values, and reflection-trapped proxy object values, and proves those malformed class values deny before supplied live-worker controls are inspected.
- Revoked proxy-backed built-in authorization-wrapper impostor coverage proves revoked map, set, weak collection, typed array, data view, promise, boxed primitive, Date, RegExp, Error, WeakRef, and FinalizationRegistry wrapper evidence denies cleanly before supplied live-worker controls are inspected or built-in metadata can be used as fallback evidence.
- Exact-field, proxy-backed, and revoked proxy-backed non-ordinary authorization-wrapper coverage proves null-prototype, class-instance, array-shaped, and function-shaped wrapper impostors deny before supplied controls are inspected or wrapper fields are read.
- Accessor-backed authorization-wrapper public-field coverage proves `workerDeploymentClass` and `controls` getters deny without getter execution or supplied-control inspection.
- Authorization-wrapper reflection-trap coverage proves wrapper prototype, descriptor, key, and frozen-state traps deny cleanly before hostile supplied controls are inspected. Revoked proxy-backed plain authorization-wrapper coverage plus own/inherited `Symbol.toStringTag` metadata and inherited/own coercion-hook wrapper coverage, including accessor-backed own coercion metadata, now also prove denial happens before hostile supplied controls are inspected.
- Exact-field built-in authorization-wrapper coverage proves map, set, weak collection, array buffer, shared array buffer when runtime-supported, typed array, data view, promise, boxed primitive, Date, RegExp, Error, WeakRef, and FinalizationRegistry wrapper evidence with exact-looking frozen public data descriptors denies before supplied live-worker controls are inspected.
- Reflection-trapped built-in authorization-wrapper coverage proves map, set, weak collection, array buffer, shared array buffer when runtime-supported, typed array, data view, promise, boxed primitive, Date, RegExp, Error, WeakRef, and FinalizationRegistry wrapper evidence denies cleanly without inspecting hostile supplied controls or letting descriptor, key, prototype, frozen-state, or get traps escape.
- URL-shaped built-in authorization-wrapper coverage proves `URL` and `URLSearchParams` evidence denies in ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied live-worker controls are inspected.
- URL-shaped built-in controls-evidence coverage proves `URL` and `URLSearchParams` deny in ordinary, proxy-backed, and revoked proxy-backed forms without fallback or object-trap inspection.
- Boxed primitive authorization-wrapper coverage now explicitly includes boxed `Symbol` and boxed `BigInt` evidence across ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied live-worker controls are inspected.
- Boxed primitive controls-evidence coverage now explicitly includes boxed `Symbol` and boxed `BigInt` evidence across ordinary, proxy-backed, and revoked proxy-backed forms before controls can be treated as the future live-worker checklist.
- Revoked proxy-backed built-in controls-evidence coverage now explicitly includes typed arrays, data views, weak collections, array buffers, shared array buffers when runtime-supported, URL-shaped records, boxed `Symbol`/`BigInt` records, weak references, and finalization registries before controls can be treated as the future live-worker checklist.
- Runtime-supported Web-platform impostor coverage now includes `Blob`, `File`, `FormData`, `Headers`, `Request`, `Response`, `AbortController`, `AbortSignal`, `ReadableStream`, `WritableStream`, `TransformStream`, `TextEncoder`, `TextDecoder`, `TextEncoderStream`, `TextDecoderStream`, `EventTarget`, `Event`, `DOMException`, `MessageChannel`, `MessagePort`, `BroadcastChannel`, `CompressionStream`, `DecompressionStream`, `CountQueuingStrategy`, `ByteLengthQueuingStrategy`, `URLPattern`, and `PerformanceObserver` as ordinary, proxy-backed, and revoked proxy-backed controls evidence plus authorization wrappers before the reserved live-worker class can inspect or authorize supplied controls.
- Runtime-supported WebAssembly impostor coverage now includes module, memory, global, and table records where available, as ordinary, proxy-backed, and revoked proxy-backed focused controls evidence plus authorization-wrapper inputs before the reserved live-worker class can authorize.
- Runtime-supported Web Crypto impostor coverage now includes `CryptoKey` records where available, as ordinary, proxy-backed, and revoked-proxy controls evidence plus ordinary, exact-field, proxy-backed, reflection-trapped, and revoked-proxy authorization-wrapper inputs before the reserved live-worker class can authorize; the revoked controls-evidence path also proves `get`, prototype, descriptor, and key traps are not inspected.
- Runtime-supported typed-array family coverage now includes every available typed-array constructor as deployment-class evidence including proxy-backed and revoked proxy-backed deployment-class evidence, `controls` evidence, and authorization-wrapper impostors before the reserved live-worker class can authorize.
- Boxed `Symbol` and boxed `BigInt` deployment-class impostor coverage proves those object-shaped values deny before supplied live-worker controls are inspected.
- Proxy-backed and revoked proxy-backed boxed primitive deployment-class impostor coverage proves boxed string, boolean, number, `Symbol`, and `BigInt` records deny before supplied controls are inspected, without reading object traps.
- Proxy-backed and revoked proxy-backed array-buffer-shaped deployment-class impostor coverage proves `ArrayBuffer`, runtime-supported `SharedArrayBuffer`, and `DataView` records deny before supplied controls are inspected, without reading object traps.
- Proxy-backed and revoked proxy-backed URL-shaped, weak-reference, and finalization-registry deployment-class impostor coverage proves `URL`, `URLSearchParams`, `WeakRef`, and `FinalizationRegistry` records deny before supplied controls are inspected, without reading object traps.
- Proxy-backed and revoked proxy-backed collection deployment-class impostor coverage proves `Map`, `Set`, `WeakMap`, and `WeakSet` records deny before supplied controls are inspected, without reading object traps.
- Proxy-backed and revoked proxy-backed Date deployment-class impostor coverage proves Date records deny before supplied controls are inspected, without reading object traps.
- Proxy-backed and revoked proxy-backed promise and error-shaped deployment-class impostor coverage proves `Promise`, `RegExp`, `Error`, and `TypeError` records deny before supplied controls are inspected, without reading object traps.
- Array-buffer, URL-shaped, weak-reference, and finalization-registry deployment-class impostor coverage proves those object-shaped values deny before supplied live-worker controls are inspected.
- Tagged-accessor deployment-class impostor coverage proves object-shaped values with own or inherited throwing `Symbol.toStringTag` accessors deny before supplied live-worker controls are inspected or the accessor is read.

## Next Best Work

1. Keep the product demo path stable while collecting review feedback.
2. Keep hardening the executable `production-live-campaign` controls before adding any supported live worker class or live-provider campaign sends.

## Guardrails

- Do not weaken live SMS, billing, AI, provider, secret, destructive database, compliance, spam, data leakage, or financial-cost gates.
- Do not expand read-only operations pages unless they directly support release safety or product work.
- Prefer existing APIs, validation helpers, tenant helpers, and tests.
- Keep `npm run validate` green before handoff.
