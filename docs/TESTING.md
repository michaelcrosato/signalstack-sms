# Testing

Milestone 0 validation runs:

- Contract presence, API documentation drift, and tenant `orgId` invariant checks
- Secret/default safety check
- ESLint
- TypeScript
- Prisma validation and client generation
- Vitest smoke
- Playwright smoke
- Next build

## M2 Tenant-Boundary Gates

PostgreSQL tenant enforcement is mandatory evidence, not an optional RLS mode:

- `npm run test:tenant-db` runs the eight-file / 33-test M2 gate against a disposable owner-capable
  PostgreSQL database. It creates non-owner NOINHERIT runtime roles and tenant A/B fixtures, then covers
  all 27 protected tables for scoped reads/writes, missing context, cross-tenant forgery, relation
  constraints/triggers, rollback, semantic runtime-policy fingerprints, exact command-specific control
  denial, worker dispatch, and multi-connection pool reuse.
- The tenant gate includes a fresh install of all 40 migrations under a non-superuser/non-BYPASSRLS
  table owner using `signalstack_owner`. That proof exercises the historical triggers and dispatch
  function and verifies the dispatch function's fixed search path and revoked `PUBLIC` EXECUTE ACL.
- The complete `RUN_DB_TESTS=true` database-directory run is 37 files / 186 tests, and the focused auth
  database run is nine files / 38 tests. Fresh migration deployment leaves no Prisma schema diff.
  CI/premerge supply PostgreSQL and keep the tenant gate inside the protected validation path.
- `npm run tenant:boundary:check` statically inventories direct Prisma imports under `app`, `lib`, and
  `workers`. The accepted M2 posture is zero unauthorized/stale entries and zero
  `tenant-migration-debt` imports; new tenant paths must use the transaction-context seam.
- `npm run test:e2e:local-auth:production` uses distinct loopback `MIGRATION_DATABASE_URL` and
  `DATABASE_URL` credentials. The owner connection manages fixtures, while the production build/server
  receives only the non-owner web login and proves the authenticated browser path through forced RLS.

Focused database command:

```powershell
$env:DATABASE_URL='<disposable PostgreSQL owner URL>'
npm run test:tenant-db
```

Full database command:

```powershell
$env:RUN_DB_TESTS='true'
$env:DATABASE_URL='<disposable PostgreSQL owner URL>'
npx vitest run tests/unit/db
```

Latest default unit-suite evidence is 150 passing / 13 skipped files and 1,078 passing / 61 skipped tests
(`npm test`; 1,139 tests total). The production local-auth build/browser proof is 1/1 under the distinct
non-owner web login.

Additional deterministic checks:

- Smoke coverage verifies the exported demo-safe runtime defaults are runtime-frozen before the root launch page, health endpoint, compliance check, and local environment views consume them, so caller-side mutation cannot drift demo mode, live messaging, live billing, dummy provider, or fake AI defaults.
- Provider settings update route tests verify role denials return before request-body parsing, and successful admin requests persist only local credential metadata before rendering secret-safe provider settings without provider number writes, credential deletion, or live-test SMS helpers.
- Static mutating API authorization coverage verifies default-initialized destructuring of `globalThis` `Request` constructor aliases, including `const { Request: RequestCtor = Request } = globalThis`, `const { [requestConstructorName]: RequestCtor = Request } = globalThis`, `({ Request: RequestCtor = Request } = globalThis)`, `({ [requestConstructorName]: RequestCtor = Request } = globalThis)`, `const { Request: RequestCtor = Request } = (globalThis as typeof globalThis)`, `const { [requestConstructorName]: RequestCtor = Request } = (globalThis satisfies typeof globalThis)`, `({ Request: RequestCtor = Request } = (globalThis as typeof globalThis))`, `({ [requestConstructorName]: RequestCtor = Request } = (globalThis satisfies typeof globalThis))`, and defaulted destructuring through local `globalThis` root aliases such as `const root = globalThis; const { Request: RequestCtor = Request } = root`, `root = (globalThis as typeof globalThis); ({ Request: RequestCtor = Request } = root)`, `root = (globalThis as typeof globalThis); ({ [requestConstructorName]: RequestCtor = Request } = root)`, and `root = (globalThis satisfies typeof globalThis); ({ [requestConstructorName]: RequestCtor = Request } = root)`, is treated as `Request.prototype` body-reader access before each handler's own top-level `requireApiRole` call.
- Static mutating API authorization coverage verifies assigned type-asserted and `satisfies` computed destructuring of `globalThis` `Object`/`Reflect` aliases, including `({ [objectName]: ObjectBuiltin, [reflectName]: ReflectBuiltin } = (globalThis as typeof globalThis))` and `({ [objectName]: ObjectBuiltin, [reflectName]: ReflectBuiltin } = (globalThis satisfies typeof globalThis))`, is treated as descriptor/prototype body-reader access before each handler's own top-level `requireApiRole` call.
- Static mutating API authorization coverage verifies default-initialized destructuring of `globalThis` `Object`/`Reflect` aliases, including `const { Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = globalThis`, `const { [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = globalThis`, `({ Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = globalThis)`, `({ [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = globalThis)`, `const { Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = (globalThis as typeof globalThis)`, `const { [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = (globalThis satisfies typeof globalThis)`, `({ Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = (globalThis as typeof globalThis))`, `({ [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = (globalThis satisfies typeof globalThis))`, and defaulted destructuring through local `globalThis` root aliases such as `const root = globalThis; const { Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = root`, `root = (globalThis as typeof globalThis); ({ Object: ObjectBuiltin = Object, Reflect: ReflectBuiltin = Reflect } = root)`, `root = (globalThis as typeof globalThis); ({ [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = root)`, and `root = (globalThis satisfies typeof globalThis); ({ [objectName]: ObjectBuiltin = Object, [reflectName]: ReflectBuiltin = Reflect } = root)`, is treated as descriptor/prototype body-reader access before each handler's own top-level `requireApiRole` call.
- Static mutating API authorization coverage verifies transitive local `globalThis` root aliases, including `const root = globalThis; const platform = root; const { Request: RequestCtor = Request } = platform`, `platform = root as typeof globalThis; ({ [requestConstructorName]: RequestCtor = Request } = platform)`, comma-declared roots such as `const root = globalThis, platform = root`, multi-hop comma-declared roots such as `const root = globalThis, platform = root, runtime = platform`, multi-hop comma-declared non-null roots such as `const root = globalThis, platform = root!, runtime = platform!` and `const root = globalThis, platform = root!, runtime = platform! satisfies typeof globalThis`, and matching defaulted `Object`/`Reflect` destructuring, resolve before prototype/descriptor body-reader calls. Non-null transitive aliases such as `const platform = root!`, `platform = root! satisfies typeof globalThis`, and `platform = root! as typeof globalThis`, comma-declared non-null forms such as `const root = globalThis, platform = root!`, combined comma-declared parenthesized non-null forms such as `const root = globalThis, platform = (root)! as typeof globalThis` and `const root = globalThis, platform = ((root as typeof globalThis)!)`, plus parenthesized non-null forms such as `const platform = (root)! as typeof globalThis`, `platform = ((root)! satisfies typeof globalThis)`, destructuring from `(platform)! as typeof globalThis`, and whole-parenthesized asserted non-null forms such as `const platform = ((root as typeof globalThis)!)`, are also treated as the same local root chain before `Request`, `Object`, or `Reflect` destructuring is checked.
- Static mutating API authorization coverage verifies multi-hop comma-declared parenthesized non-null local `globalThis` root aliases, including `const root = globalThis, platform = (root)! as typeof globalThis, runtime = ((platform as typeof globalThis)!)` and `const root = globalThis as typeof globalThis, platform = ((root as typeof globalThis)!), runtime = ((platform satisfies typeof globalThis)!)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies multi-hop comma-declared whole-parenthesized non-null local `globalThis` root aliases, including `const root = (globalThis)!, platform = ((root)!), runtime = ((platform)!)` and `const root = ((globalThis as typeof globalThis)!), platform = ((root)! satisfies typeof globalThis), runtime = ((platform)! as typeof globalThis)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies mixed declaration and assignment multi-hop local `globalThis` root aliases, including `const root = globalThis; platform = root; const runtime = platform`, `const root = (globalThis) as typeof globalThis; platform = root satisfies typeof globalThis; const runtime = platform as typeof globalThis`, and `const root = ((globalThis as typeof globalThis)!); platform = ((root)! satisfies typeof globalThis); runtime = ((platform)! as typeof globalThis)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies `let` and `var` mixed declaration/assignment multi-hop local `globalThis` root aliases, including `let root = globalThis; var platform = root; runtime = platform`, `let root = (globalThis as typeof globalThis); var platform = ((root)! satisfies typeof globalThis); runtime = platform as typeof globalThis`, and `var root = globalThis satisfies typeof globalThis; let platform = root!; var runtime = platform! as typeof globalThis`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies multi-hop assigned transitive local `globalThis` root aliases, including `root = globalThis; platform = root; runtime = platform` and `root = globalThis as typeof globalThis; platform = root satisfies typeof globalThis; runtime = platform as typeof globalThis`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies multi-hop assigned non-null local `globalThis` root aliases, including `root = globalThis; platform = root!; runtime = platform!` and `root = globalThis as typeof globalThis; platform = root! satisfies typeof globalThis; runtime = platform! as typeof globalThis`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies multi-hop assigned parenthesized non-null local `globalThis` root aliases, including `root = globalThis; platform = (root)! as typeof globalThis; runtime = ((platform as typeof globalThis)!)` and `root = globalThis as typeof globalThis; platform = ((root as typeof globalThis)!); runtime = ((platform satisfies typeof globalThis)!)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies multi-hop assigned whole-parenthesized non-null local `globalThis` root aliases, including `root = (globalThis)!; platform = ((root)!); runtime = ((platform)!)` and `root = ((globalThis as typeof globalThis)!); platform = ((root)! satisfies typeof globalThis); runtime = ((platform)! as typeof globalThis)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies parenthesized assignment-expression non-null multi-hop local `globalThis` root aliases, including `((root = (globalThis)!)); ((platform = ((root)!))); ((runtime = ((platform)!)))`, `((root = ((globalThis as typeof globalThis)!))); ((platform = ((root)! satisfies typeof globalThis))); ((runtime = ((platform)! as typeof globalThis)))`, and comma-expression chains such as `((root = globalThis), (platform = root), (runtime = platform))`, `(((root = (globalThis)!), (platform = ((root)!)), (runtime = ((platform)!))))`, and `(((root = ((globalThis as typeof globalThis)!)), (platform = ((root)! satisfies typeof globalThis)), (runtime = ((platform)! as typeof globalThis))))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies nullish/fallback/logical-and assignment local `globalThis` root aliases, including `root ??= globalThis; platform ||= root; runtime ??= platform`, `root &&= globalThis; platform &&= root; runtime &&= platform`, `((root ??= ((globalThis as typeof globalThis)!))); ((platform ||= ((root)! satisfies typeof globalThis))); ((runtime ??= ((platform)! as typeof globalThis)))`, and comma-expression chains such as `((root ??= globalThis), (platform ||= root), (runtime &&= platform))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies simple sequence-expression local `globalThis` root aliases, including `root = (void 0, globalThis); platform = (undefined, root); runtime = (0, platform)` plus typed/non-null variants, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies comma-declared sequence-expression local `globalThis` root aliases, including `const root = (void 0, globalThis), platform = (undefined, root), runtime = (0, platform)` plus typed/non-null variants, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies conditional-expression local `globalThis` root aliases where both branches are known roots, including `root = condition ? globalThis : globalThis; platform = condition ? root : root; runtime = true ? platform : platform`, comma-declared forms such as `const root = condition ? globalThis : globalThis, platform = condition ? root : root, runtime = true ? platform : platform`, plus typed/non-null variants, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies logical-expression local `globalThis` root aliases where both operands are known roots, including `root = globalThis || globalThis; platform = root ?? root; runtime = platform && platform`, comma-declared forms such as `const root = globalThis || globalThis, platform = root ?? root, runtime = platform && platform`, plus typed/non-null variants, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies sequence-wrapped conditional and logical local `globalThis` root aliases, including `root = (void 0, condition ? globalThis : globalThis); platform = (undefined, condition ? root : root); runtime = (0, true ? platform : platform)` and `root = (void 0, globalThis || globalThis); platform = (undefined, root ?? root); runtime = (0, platform && platform)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies comma-declared sequence-wrapped conditional and logical local `globalThis` root aliases, including `const root = (void 0, condition ? globalThis : globalThis), platform = (undefined, condition ? root : root), runtime = (0, true ? platform : platform)`, typed forms such as `const root: typeof globalThis = (void 0, condition ? globalThis : globalThis), platform: typeof globalThis = (undefined, condition ? root : root), runtime: typeof globalThis = (0, true ? platform : platform)`, and `const root = (void 0, globalThis || globalThis), platform = (undefined, root ?? root), runtime = (0, platform && platform)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies asserted typed comma-declared sequence-wrapped local `globalThis` root aliases, including `const root: typeof globalThis = (void 0, ((globalThis as typeof globalThis)!)), platform: typeof globalThis = (undefined, ((root)! satisfies typeof globalThis)), runtime: typeof globalThis = (0, ((platform as typeof globalThis)!))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies sequence-wrapped logical-assignment local `globalThis` root aliases, including `root ??= (void 0, globalThis); platform ||= (undefined, root); runtime &&= (0, platform)` plus typed/non-null variants, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies sequence-wrapped conditional/logical expression right-hand sides inside local `globalThis` logical-assignment chains, including `root ??= (void 0, condition ? globalThis : globalThis); platform ||= (undefined, root ?? root); runtime &&= (0, platform && platform)`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies comma-expression chains with sequence-wrapped conditional/logical right-hand sides inside local `globalThis` logical-assignment aliases, including `((root ??= (void 0, condition ? globalThis : globalThis)), (platform ||= (undefined, root ?? root)), (runtime &&= (0, platform && platform)))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies asserted typed sequence-wrapped expression right-hand sides inside local `globalThis` logical-assignment aliases, including `let root: typeof globalThis; let platform: typeof globalThis; let runtime: typeof globalThis; root ??= (void 0, ((globalThis as typeof globalThis)!)); platform ||= (undefined, ((root)! satisfies typeof globalThis)); runtime &&= (0, ((platform as typeof globalThis)!))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.
- Static mutating API authorization coverage verifies asserted typed comma-expression chains with sequence-wrapped right-hand sides inside local `globalThis` logical-assignment aliases, including `let root: typeof globalThis; let platform: typeof globalThis; let runtime: typeof globalThis; (((root ??= (void 0, ((globalThis as typeof globalThis)!))), (platform ||= (undefined, ((root)! satisfies typeof globalThis))), (runtime &&= (0, ((platform as typeof globalThis)!)))))`, resolve before defaulted `Request`, `Object`, or `Reflect` destructuring reaches prototype/descriptor body-reader calls.

- Playwright starts the local Next.js test server on `127.0.0.1:3100` by default, separate from the normal dev port. Set `PLAYWRIGHT_PORT` to another integer port when a local process already uses that port. Existing-server reuse is opt-in with `PLAYWRIGHT_REUSE_EXISTING_SERVER=true`.
- `npm run context:check` is part of `npm run validate` and keeps current agent handoff files compact. It also verifies that current loop guidance points agents at `npm run agent:brief` before broad historical reads. Run history lives in `git log`; large tests should be read with targeted search.
- `npm run agent:brief` prints a compact autonomous-loop startup packet with current git state, recent commits, current handoff sections, handoff sizes, latest run headings, and large-file advisories. It is tested directly for loop startup use and is intentionally not part of `npm run validate`.
- GitHub `ci` and `premerge` workflows call the protected local gate with PowerShell instead of treating direct `npm run validate` or `npm run premerge` calls as green, so workflow validation includes gate integrity verification and demo-safe defaults.
- `npm run test:e2e:demo` runs the investor demo path after the local database has been migrated and seeded.
- `npm run test:e2e:product-demo` runs the seeded product dashboard, contacts, campaign, campaign detail, inbox, template, analytics, and compliance paths for `/dashboard`, `/dashboard/contacts`, `/dashboard/campaigns`, `/dashboard/campaigns/:campaignId`, `/dashboard/inbox`, `/dashboard/templates`, `/dashboard/analytics`, and `/dashboard/compliance` after the local database has been migrated and seeded. The dashboard path verifies read-only next-step links plus seeded local delivery review and latest-evidence wording from delivered, pending, and failed dummy evidence rows; the contacts path imports a local CSV row through `POST /api/contacts/imports`; the campaign path creates, preflights with visible allowed/blocked recipient counts, schedules, shows campaign-list delivery review status plus latest outbound evidence labels, shows recipient send-state/human-readable block-reason detail, shows aggregate outbound delivery metrics plus explicit recent-row boundary copy, visible recent-evidence row count, newest-first recent local message metadata, provider error-code evidence, edits a draft, and cancels a queued local campaign; the inbox path creates a local inbound reply, adds a note, and resolves/reopens a local thread; the template path creates reusable local copy; the analytics path reads existing local overview totals, including scheduled-campaign, outbound-delivery counts, latest outbound evidence, and campaign-level delivery review summary/links with seeded failed and pending examples, then links to failed-campaign provider error-code evidence without SMS, provider calls, billing, live AI, report execution, exports, mutations, delivery retries, worker execution, message delivery mutation, or live messaging enablement.
- Unit coverage verifies shared product dashboard navigation metadata is frozen at both array and entry level before product pages render it, so caller-side item mutation cannot drift product labels or route targets.
- Unit coverage verifies product dashboard action metadata is frozen at both array and entry level before `/dashboard` renders header actions, so caller-side item mutation cannot drift demo-console or settings link labels, route targets, or button intent.
- Unit coverage verifies product dashboard metric metadata is frozen at both array and entry level before `/dashboard` renders it, so caller-side item mutation cannot drift active-contact, campaign, open-conversation, or template metric labels and order.
- Unit coverage verifies product dashboard local signal metadata is frozen at both array and entry level before `/dashboard` renders analytics pills, so caller-side item mutation cannot drift consent coverage, opt-in rate, scheduled work, delivery evidence, delivery rate, delivery pending, delivery failures, delivery review, last delivery evidence, inbox load, fake-AI request, or local usage labels and order.
- Unit coverage verifies `/dashboard` and `/dashboard/analytics` use shared outbound delivery-count query helpers for local total, delivered, pending, and failed message buckets; helper coverage pins mutually exclusive terminal-failure handling and fresh provider-status arrays so caller mutation cannot drift later counts.
- Unit coverage verifies product dashboard next-step metadata is frozen at both array and entry level before `/dashboard` renders owner-facing inbox, campaign, and compliance links, so caller-side item mutation cannot drift local demo guidance.
- Unit coverage verifies product dashboard compliance readiness counts derive from the shared frozen product compliance checklist metadata, so `/dashboard` and `/dashboard/compliance` cannot drift on required profile fields.
- Unit coverage verifies product dashboard section/status metadata is frozen at section array, section entry, row array, and row entry level before `/dashboard` renders contacts, compliance, campaigns, inbox, and templates summary sections, so caller-side mutation cannot drift section titles, row labels, or order.
- Unit coverage verifies product contact metric metadata is frozen at both array and entry level before `/dashboard/contacts` renders it, so caller-side item mutation cannot drift active, consent, or archived metric labels and order.
- Unit coverage verifies product contact detail status metadata is frozen at both array and entry level before `/dashboard/contacts/:contactId` renders it, so caller-side item mutation cannot drift phone, consent, list, tag, or archived status labels and order.
- Unit coverage verifies product contact consent option metadata is frozen at both array and entry level before `/dashboard/contacts/:contactId` renders it, so caller-side item mutation cannot drift consent select values or order.
- Unit coverage verifies product contact import defaults are frozen before `/dashboard/contacts` renders the local CSV import workflow, so caller-side mutation cannot drift the default demo filename or local opt-in CSV row.
- Unit coverage verifies product campaign metric metadata is frozen at both array and entry level before `/dashboard/campaigns` renders it, so caller-side item mutation cannot drift total, draft, scheduled, or ready-recipient metric labels and order.
- Unit coverage verifies product campaign list rows project outbound-only local delivery counts into delivered, pending, failed, delivery-rate, review-status, and latest outbound evidence summaries before `/dashboard/campaigns` renders them.
- Unit coverage verifies product campaign detail lifecycle metric metadata is frozen at both array and entry level before `/dashboard/campaigns/:campaignId` renders it, so caller-side item mutation cannot drift status, recipient, template, or schedule metric labels and order.
- Unit coverage verifies product campaign recipient snapshot metadata is frozen at both array and entry level before `/dashboard/campaigns/:campaignId` renders it, so caller-side item mutation cannot drift Consent, Archive, Send State, or Block Reason status labels and order.
- Unit coverage verifies product campaign delivery metric metadata is frozen at both array and entry level before `/dashboard/campaigns/:campaignId` renders it, so caller-side item mutation cannot drift Outbound Messages, Recent Evidence Rows, Delivery Rate, Delivered, Pending, Failed, Last Outbound Message, Provider Statuses, or Provider Error Codes labels and order.
- Unit coverage verifies product campaign detail delivery rows and delivered/pending/failed/provider-status/provider-error-code metrics ignore inbound campaign-linked messages, that recent evidence rows render newest-first, and that detail metrics aggregate all outbound campaign messages separately from the recent visible row cap, so local campaign delivery snapshots stay outbound-only and complete.
- Unit coverage verifies product campaign composer defaults are frozen before `/dashboard/campaigns` renders them, so caller-side mutation cannot drift the default local campaign name, STOP-safe fallback body, fake-AI prompt, business-name, or tone.
- Unit coverage verifies product inbox metric metadata is frozen at both array and entry level before `/dashboard/inbox` renders it, so caller-side item mutation cannot drift total, open, resolved, or recent-inbound thread metric labels and order.
- Unit coverage verifies product inbox thread status metadata is frozen at both array and entry level before `/dashboard/inbox` renders it, so caller-side item mutation cannot drift selected-thread Thread and Consent labels or order.
- Unit coverage verifies product inbox workspace defaults are frozen before `/dashboard/inbox` renders local reply and note forms, so caller-side mutation cannot drift the default demo inbound reply or internal-note copy.
- Unit coverage verifies product template metric metadata is frozen at both array and entry level before `/dashboard/templates` renders it, so caller-side item mutation cannot drift saved-template, variable, campaign-usage, or live-send blocker metric labels and order.
- Unit coverage verifies product template detail metric metadata is frozen at both array and entry level before `/dashboard/templates/:templateId` renders it, so caller-side item mutation cannot drift variable, campaign-usage, updated, or live-send blocker lifecycle labels and order.
- Unit coverage verifies product template create-form defaults are frozen before `/dashboard/templates` renders them, so caller-side mutation cannot drift the default local template name or STOP-safe body copy.
- Unit coverage verifies product compliance metric metadata is frozen at both array and entry level before `/dashboard/compliance` renders it, so caller-side item mutation cannot drift profile-field, A2P, live-messaging, or blocker metric labels and order.
- Unit coverage verifies product compliance checklist metadata is frozen at both array and entry level before `/dashboard/compliance` renders it, so caller-side item mutation cannot drift readiness labels or guidance.
- Unit coverage verifies product compliance hard-gate blocker copy metadata is frozen before `/dashboard/compliance` renders it, so caller-side mutation cannot drift blocker descriptions.
- Unit coverage verifies product analytics metric metadata is frozen at both array and entry level before `/dashboard/analytics` renders it, so caller-side item mutation cannot drift consent coverage, campaign, inbox-load, or usage-event metric labels and order.
- Unit coverage verifies product analytics usage-row metadata is frozen at both array and entry level before `/dashboard/analytics` renders it, so caller-side item mutation cannot drift local usage labels or ordering.
- Unit coverage verifies product analytics delivery-row metadata is frozen at both array and entry level before `/dashboard/analytics` renders it, so caller-side item mutation cannot drift outbound, delivered, pending, failed, delivery-rate, review-status, or latest-evidence rows. Product analytics coverage also verifies campaign-level delivery review rows are derived from outbound local message evidence, ordered by failed/pending review priority, and capped with visible/hidden summary counts. Analytics overview coverage keeps delivered, pending, failed, and latest-evidence lookups scoped to outbound messages through the shared delivery-count query helpers.
- Contract drift tests verify the route scanner covers all supported Next API route methods, including `HEAD` and `OPTIONS`, so newly implemented handlers cannot bypass `contracts/CONTRACT-API.md` and `docs/API_MAP.md` documentation. The scanner ignores route export mentions inside comments, quoted strings, template literals, and regex literals, including escaped regex bodies and character classes, so examples cannot create false implemented-endpoint inventory entries.
- `npm run worker` processes local due scheduled campaign jobs through the dummy provider only and is blocked in production-like runtimes. Unit coverage verifies all production-like runtime markers (`NODE_ENV`, `VERCEL_ENV`, `DEPLOYMENT_ENV`, and `APP_ENV`) block before provider or future live-worker-class checks can fall through.
- `npm run worker:watch` runs the same worker in continuous polling mode for local demos. It is still blocked unless `MESSAGING_PROVIDER=dummy`, live messaging is disabled, and no production-like runtime marker is present.
- Worker readiness accepts only an unset `WORKER_DEPLOYMENT_CLASS` or `WORKER_DEPLOYMENT_CLASS=local-demo`; any future-looking production/live class is blocked before database or BullMQ workers can process scheduled campaign jobs, including the reserved planning-only `production-live-campaign` class. Worker provider readiness also accepts only unset, empty, or exactly `false` `LIVE_MESSAGING_ENABLED` values with the dummy provider; malformed live-messaging flag values fail closed.
- Scheduled-campaign worker processing coverage verifies send-time preflight skips recipients whose consent/archive state changed after scheduling, marks those recipients `BLOCKED`, and sends only allowed recipients through the dummy provider.
- Live-worker control metadata tests verify that `production-live-campaign` stays outside the supported worker class list, that the future control checklist is runtime-frozen, and that the reserved class can be authorized only when the authorization wrapper is an ordinary object with no hidden required fields, inherited-field, or inherited-extra-field influence, exposes frozen data fields for `workerDeploymentClass` and `controls`, the supplied control array and entries are frozen, the entries are ordinary object records, the array exposes only enumerable indexed entries plus its native `length` in exact ordinary array key order, the `length` descriptor matches the exact checklist size, the exact frozen checklist IDs and requirement text are present in order, every control exposes only own enumerable data fields for the public `id`, `status`, and `requirement` fields, every control status is inside the supported vocabulary, and every listed control is explicitly implemented. Direct identity/status predicates also reject getter-backed public fields without executing those getters, hidden string, data-backed `Symbol.toStringTag`, custom iterator, data-backed custom iterator, custom async-iterator, own array method-name, or symbol control-array metadata, hidden string or symbol control-entry metadata, hidden required control fields, malformed primitive control entries, non-ordinary array/date/function-shaped control entries, inherited data-backed indexed slots, inherited accessor indexed slots, accessor-backed indexed slots, non-enumerable indexed control slots, writable/configurable indexed control slots, proxy-invalid indexed descriptors, sealed-but-writable, configurable public-field, duplicate-key, nullish public-field values, boxed string public-field values, case-drifted or whitespace-padded public strings including spaces, tabs, newlines, carriage returns, CRLF pairs, and invisible Unicode escape padding, or proxy-invalid public-field-descriptor control entries, and missing, accessor-backed, writable, proxy-invalid configurable, or proxy-invalid enumerable native length descriptors remain unauthorized, boxed numeric, mismatched or oversized safe-integer, malformed primitive, or hostile array `length` descriptors including boolean, bigint, symbol, string, and non-safe numeric values deny cleanly without coercing descriptor values, leaking descriptor invariant errors, or trusting indexed controls, malformed deployment-class primitive values including bigint, malformed object values including inherited-coercion-hook objects, and blank or whitespace-only deployment-class strings deny without coercion or supplied-control inspection, function-shaped controls evidence denies without invocation, object-shaped non-array controls evidence including proxy-backed maps, proxy-backed sets, proxy-backed weak collections, proxy-backed typed arrays, proxy-backed data views, proxy-backed array buffers, proxy-backed shared array buffers, proxy-backed promises, proxy-backed dates, proxy-backed regular expressions, proxy-backed errors, proxy-backed URLs, proxy-backed URLSearchParams, proxy-backed Web-platform records, proxy-backed weak references, and proxy-backed finalization registries, revoked proxy-backed built-in controls evidence including typed arrays, data views, weak collections, URL-shaped controls, and Web-platform records, hostile array-like getter or iterator shapes, proxy-backed or revoked proxy-backed array-prototype impostors, and `Symbol.toStringTag` array impostors denies without reading getters or traps, inherited array iterator hooks, transform/reducer method metadata, mutator/visitor method metadata, and copy-helper method metadata are not read while evaluating exact frozen control-array evidence, hidden string or symbol authorization-wrapper metadata remains unauthorized, own object-valued authorization-wrapper `constructor` or `toLocaleString` metadata denies without coercion or control inspection, own or inherited `Symbol.toStringTag` authorization-wrapper metadata denies before hostile controls are inspected and without reading tag getters, Date-shaped built-in authorization-wrapper impostors deny in ordinary, exact-field, proxy-backed, reflection-trapped, and revoked proxy-backed forms before control evidence is inspected, reordered or duplicate proxy keys on control arrays or authorization wrappers remain unauthorized, proxy-invalid authorization-wrapper field descriptors deny before controls are inspected, malformed proxy-backed or revoked-proxy-backed evidence denies cleanly without descriptor, key, prototype, or frozen-state reflection traps escaping on control arrays, control entries, or authorization wrappers, and descriptor-valid proxy-wrapped exact frozen evidence is evaluated through descriptors without executing array or entry `get` traps.
- Deployment-class metadata coverage proves object-shaped deployment-class values with own or inherited accessor-backed, own/inherited data-backed callable, own/inherited object-valued, or hidden own/inherited string/symbol metadata deny without reading metadata getters, invoking callable metadata or hostile coercion hooks, coercing hidden object-valued metadata, or inspecting supplied controls.
- Deployment-class coverage also proves proxy-backed and revoked proxy-backed Date evidence denies before supplied controls are inspected or object traps can authorize the reserved class.
- Live-worker authorization-wrapper coverage includes ArrayBuffer and, where runtime-supported, SharedArrayBuffer impostors in ordinary, exact-field frozen, proxy-backed, and revoked proxy-backed forms before supplied controls can be inspected.
- Live-worker authorization-wrapper coverage includes `URL` and `URLSearchParams` impostors in ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied controls can be inspected.
- Live-worker authorization-wrapper coverage includes runtime-supported Web-platform impostors such as `Blob`, `FormData`, `Headers`, `Request`, `Response`, stream objects, encoding-stream objects, event objects, `DOMException`, channel/port objects, queueing strategies, `URLPattern`, and `PerformanceObserver`, plus runtime-supported WebAssembly and Web Crypto records, in ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied controls can be inspected. Controls-evidence coverage also includes focused runtime-supported WebAssembly and revoked proxy-backed Web Crypto records before the reserved class can authorize or inspect object traps.
- Live-worker authorization-wrapper coverage includes reflection-trapped built-in impostors before supplied controls can be inspected, without descriptor, key, prototype, frozen-state, or get traps escaping.
- Live-worker control metadata tests also prove hidden string metadata on otherwise valid control arrays, control entries, or authorization wrappers remains unauthorized before the reserved class can pass, including accessor-backed or data-backed hidden control-array metadata that denies without reading or invoking it, sealed-but-writable authorization wrappers deny before supplied controls are inspected, accessor-backed authorization-wrapper public fields deny without reading getters or inspecting supplied controls, accessor-backed and data-backed `Symbol.toStringTag` authorization-wrapper metadata and inherited accessor-backed deployment-class tag metadata deny without reading tag getters, invoking callable tag metadata, or inspecting supplied controls, own or inherited data-backed callable deployment-class metadata denies without callable invocation or supplied-control inspection, inherited `Object.prototype` authorization-wrapper `Symbol.toStringTag` metadata is not read while exact frozen wrapper evidence authorizes, that non-ordinary array/date/function-shaped authorization wrappers and proxy-backed or revoked proxy-backed null-prototype/class-instance authorization-wrapper impostors deny before supplied controls are inspected, and that collection including proxy-backed and revoked proxy-backed collection evidence, boxed primitive including proxy-backed and revoked proxy-backed boxed primitive evidence, array-buffer, runtime-supported typed-array including proxy-backed and revoked proxy-backed typed-array, URL-shaped, weak-reference, finalization-registry, runtime-supported Web-platform, WebAssembly, Web Crypto, and reflection-trapped proxy object deployment-class impostors deny before supplied controls are inspected.
- Live-worker control-entry metadata coverage proves own `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` hooks on otherwise matching control entries deny without reading, invoking, or coercing those hooks, including data-backed `Symbol.toStringTag`, accessor-backed coercion metadata, data-backed coercion metadata, and object-valued metadata with hostile coercion hooks. Live-worker control-array metadata coverage proves own `constructor`, own `toLocaleString`, own `Symbol.asyncIterator`, own data-backed `Symbol.toStringTag`, iterator, async-iterator, and coercion metadata, own object-valued tag, iterator, async-iterator, well-known symbol, array method-name, coercion, `constructor`, or `toLocaleString` metadata with coercion hooks, own accessor-backed and data-backed well-known symbol metadata, own array method-name metadata, `Symbol.toPrimitive`, `toString`, and `valueOf` hooks on otherwise matching control arrays deny without reading, invoking, or coercing those hooks, and exact frozen control-array evidence remains authorized without reading inherited `Array.prototype` coercion or `Symbol.toStringTag` metadata or coercing object-valued inherited `Array.prototype` metadata.
- Live-worker authorization-wrapper metadata coverage proves own object-valued `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` metadata with hostile coercion hooks denies before supplied controls are inspected and without coercing those metadata objects.
- Live-worker authorization-wrapper helper metadata coverage proves own object-valued iterator, well-known symbol, Object-helper, legacy accessor-helper, and `__proto__` metadata with hostile coercion hooks denies before supplied controls are inspected and without coercing those metadata objects.
- Live-worker control-array iterator, lookup, quantifier, transform/reducer, mutator/visitor, copy-helper, constructor, `toLocaleString`, Object-helper, legacy accessor-helper, prototype-accessor, and coercion coverage also proves exact frozen evidence remains authorized without reading or invoking inherited `Array.prototype` Object-helper metadata (`hasOwnProperty`, `propertyIsEnumerable`, or `isPrototypeOf`), inherited `Array.prototype` legacy accessor-helper metadata (`__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, or `__lookupSetter__`), inherited `Array.prototype.__proto__` prototype-accessor metadata, without reading or invoking inherited `Array.prototype.toLocaleString` metadata, without invoking data-backed inherited index-slot metadata at occupied own checklist indexes or beyond the frozen checklist entries, non-index string or symbol metadata, `Array.prototype[Symbol.iterator]`, `Array.prototype[Symbol.asyncIterator]`, `Array.prototype[Symbol.toStringTag]`, `Array.prototype.constructor`, `Array.prototype[Symbol.unscopables]`, `Array.prototype[Symbol.isConcatSpreadable]`, string-method symbol metadata (`Symbol.match`, `Symbol.matchAll`, `Symbol.replace`, `Symbol.search`, or `Symbol.split`), iteration method metadata (`entries`, `keys`, or `values`), lookup method metadata (`at`, `includes`, `indexOf`, `lastIndexOf`, `find`, `findIndex`, `findLast`, or `findLastIndex`), quantifier metadata (`every`), transform/reducer method metadata (`filter`, `flatMap`, `map`, `reduce`, or `reduceRight`), mutator/visitor method metadata (`concat`, `copyWithin`, `fill`, `flat`, `forEach`, `join`, `pop`, `push`, `reverse`, `shift`, `slice`, `some`, `sort`, `splice`, or `unshift`), copy-helper method metadata (`toReversed`, `toSorted`, `toSpliced`, or `with`), `Array.prototype[Symbol.toPrimitive]`, `Array.prototype.toString`, or `Array.prototype.valueOf` metadata, and without coercing object-valued inherited `Array.prototype` metadata across those categories, including inherited numeric index metadata at occupied own checklist indexes or beyond the frozen checklist entries.
- Live-worker control metadata tests also prove proxy-backed non-array `controls` evidence denies without reading object `get`, prototype, descriptor, or key traps before the reserved class can pass.
- `WORKER_MAX_JOBS_PER_POLL` caps local worker throughput per poll and is clamped to a bounded range for demos/tests.
- BullMQ enqueue and worker helpers are unit-tested without requiring Redis. Coverage pins monotonic generation-based mirror IDs for reopened durable jobs, containment of construct/add/close failures, and processor retries for early, active-lease, blocked-runtime, and uncertain-processing outcomes. BullMQ worker readiness coverage also verifies all production-like runtime markers block before provider or future live-worker-class checks can fall through. Local validation must remain green with `QUEUE_BACKEND` unset or set to `database`.
- Phone lookup coverage verifies local normalization remains the default, paid Twilio Lookup requires exact live enablement plus cost acknowledgement, credentials, and a constant-time server-token match from the dedicated request header before fetch, operator secrets are not forwarded to Twilio, provider phone numbers normalize to the exact requested number, timeouts stay bounded and abort requests, explicitly requested provider/configuration failures fail closed, and CSV imports never call live lookup even when all live configuration is present.
- Production deployment gate tests verify that production-like environments cannot enable external-impact settings without an explicit future override.
- API rate limiting helpers are unit-tested with deterministic clocks and isolated stores. Local validation keeps the middleware defaults generous enough for smoke and demo paths.
- Campaign create/update route tests verify malformed JSON returns `400` before local campaign create/update repository mutations run. Contact create/update/merge/import route tests verify malformed JSON returns `400` before local contact upsert/update/merge, CSV parsing, or import repository mutations run. Inbox/demo inbound route tests verify malformed JSON returns `400` before local inbound message, conversation message, assignment, note, or resolve repository mutations run. Fake AI route tests verify malformed JSON returns `400` before conversation lookup, fake provider execution, or local `AI_REQUEST` usage metering can run. Billing usage, campaign preflight, compliance settings, provider number metadata, provider settings, and live-test SMS route tests verify malformed JSON returns `400` before local usage records, local preflight repository work, compliance updates, readiness audit writes, provider metadata writes, provider settings rendering, or gated live-test send helpers can run. Readiness audit route tests verify unsupported JSON/CSV export query filters return `400` before local audit event reads or CSV serialization, and supported bounded filters read only local audit metadata. Provider credential rotation route tests verify unsupported JSON/CSV export query filters return `400` before local rotation-history reads or CSV serialization, and supported bounded filters read only local redacted credential metadata. Compliance settings route tests verify role denials return before request-body parsing, and successful admin requests update only local compliance metadata and local readiness audit records before evaluating the local messaging hard gate without provider metadata writes or live-test SMS helpers. Provider number metadata route tests verify role denials return before request-body parsing, and successful admin requests upsert only local provider number metadata without provider settings rendering, provider credential persistence, or live-test send helpers. Provider settings delete route tests verify role denials return before local credential metadata is cleared, and successful deletion clears only local metadata before rendering secret-safe provider settings. Campaign schedule route tests verify role denials return before request-body parsing, malformed or schema-invalid schedule payloads return `400` before local queue scheduling or BullMQ enqueue helpers can run, missing campaigns return `404` without BullMQ enqueue, and valid payloads persist a local queue job before optional BullMQ enqueue. CI validates both active integration pushes (`develop`) and release pushes (`main`), as well as every pull request.
- Live-worker control tests verify exact frozen control-entry evidence remains descriptor-based and does not read accessor-backed or invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen authorization-wrapper evidence remains descriptor-based and does not read accessor-backed or invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen live-worker evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen authorization-wrapper evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` public-field metadata for `workerDeploymentClass` or `controls`, including when a wrapper is missing own `controls`.
- Live-worker control tests verify exact frozen control-entry evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` public-field metadata for `id`, `status`, or `requirement`.
- Live-worker control tests verify exact frozen authorization-wrapper and control-entry evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype[Symbol.toStringTag]` metadata.
- Live-worker control tests verify exact frozen control-array density, descriptor-enumerability, scratch-array, and frozen evidence checks remain descriptor-based and do not read or invoke inherited `Object.prototype[Symbol.iterator]`, `Object.prototype.toLocaleString`, arbitrary inherited `Object.prototype` non-public string or symbol metadata, `Object.prototype.hasOwnProperty`, `Object.prototype.propertyIsEnumerable`, `Object.prototype.isPrototypeOf`, `Object.prototype.constructor`, `Object.prototype.__proto__` prototype-accessor metadata, or legacy accessor-helper metadata (`__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, or `__lookupSetter__`).
- Live-worker control tests verify exact frozen live-worker evidence remains descriptor-based and does not coerce object-valued inherited `Object.prototype` tag, iterator, well-known symbol, Object-helper, legacy accessor-helper, prototype-accessor, constructor, `toLocaleString`, hidden string or symbol, or coercion metadata.
- Live-worker control tests verify exact frozen control-array evidence remains descriptor-based and does not coerce object-valued inherited `Array.prototype` numeric index metadata at occupied own checklist indexes or beyond the frozen checklist entries.
- Live-worker control tests verify sparse supplied control arrays remain unauthorized when inherited `Array.prototype` index slots carry object-valued metadata with hostile coercion hooks, and those hooks are not coerced.
- Settings surface allowlist tests pin the ten implemented child pages under `/settings`, reject static app links to non-allowlisted settings paths, and verify that every allowlisted path has a backing page.
- Shared operator-surface inventory tests keep `/settings`, `/demo`, and the ten child pages aligned with the root launch view, demo console, settings directory, operations index, and runbook navigation.
- Seeded browser coverage exercises the consolidated `/settings` readiness view, `/demo`, and each surviving operator page without provider calls, billing, notifications, mutations, secrets, or live-feature enablement.
- Product browser coverage exercises the corresponding interactive workflows under `/dashboard/contacts`, `/dashboard/campaigns`, `/dashboard/inbox`, `/dashboard/templates`, `/dashboard/analytics`, and `/dashboard/compliance`.
- Provider, compliance, queue, exports, readiness-audit, validation, security, health, and runbook API/page tests continue to verify their focused safety boundaries.

- Campaign cancel route unit coverage verifies role denials return before local cancellation, missing campaigns return `404`, expected state/processing races return controlled `409` responses, unexpected persistence details are replaced by a generic `500`, and successful cancellation returns the locally paused campaign without provider calls, BullMQ enqueueing, worker execution, billing, notifications, SMS, or live feature enablement. Repository coverage verifies only scheduled campaigns can be locally paused by cancellation; missing campaigns return without queue mutations, while draft, paused, and completed campaigns reject without queue or campaign mutations.
- Campaign preflight repository coverage verifies missing campaigns return before contact reads and missing or cross-tenant requested contact IDs are returned as blocked `CONTACT_NOT_FOUND` rows. Campaign schedule/cancel coverage verifies missing and non-schedulable campaigns return before queue mutations, failed preflight creates no job, active processing blocks cancel/reschedule, and same-timestamp canceled jobs reopen only through a status-guarded generation increment. Worker coverage verifies concurrent database/BullMQ consumers use one tenant-scoped owner-token lease before provider calls, losing workers skip, expired processing leases recover, ownership renews before sends, only the owner can finish/fail, queue plus campaign terminal states commit in one guarded transaction, unexpected errors pause the scheduled campaign, stale schedules cancel without sends, and outbound provider/message idempotency remains tenant-explicit. A Postgres-backed regression forces both cancellation/claim lock orders and proves cancellation wins with a zero-row claim or the processing claim wins with cancellation rejected and rolled back.
- Contact consent-evidence integration coverage verifies the database permits first capture and identical rewrites, rejects clearing or replacement, and allows exactly one of two concurrent first-capture writers to commit.
- Template create/update route unit coverage verifies malformed JSON returns `400` before local template upsert/update repository mutations run.
- Twilio webhook helper coverage verifies unsupported request body formats return a controlled invalid form payload result before signature validation, while URL-encoded form payloads preserve unknown provider fields for signature validation and raw local storage.
- Twilio webhook repository and route coverage verifies malformed forms and invalid signatures return before tenant lookup or local mutations, concurrent unprocessed duplicates cannot steal a live owner lease and receive `409` plus advisory `Retry-After`, processed duplicates return `204`, downstream failures release only the owner's claim, stale leases recover, inbound routes disable AI/automatic replies, and status updates are scoped by the current tenant plus provider message ID. A Postgres-backed concurrency test verifies exactly one simultaneous claim winner.
- Delivery-status, webhook-route, delivery-count, and Prometheus exporter coverage verifies `failed`, `undelivered`, and `canceled` share one terminal-failure vocabulary. Prometheus coverage also creates another tenant's failure evidence and verifies the current-organization scrape excludes it and excludes the unattributable process-global signature-failure counter.
