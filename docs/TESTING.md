# Testing

Milestone 0 validation runs:

- Contract presence, API documentation drift, and tenant `orgId` invariant checks
- Secret/default safety check
- ESLint
- TypeScript
- Prisma validation and client generation
- Vitest smoke
- Playwright smoke
- Next build

Additional deterministic checks:

- Provider settings update route tests verify role denials return before request-body parsing, and successful admin requests persist only local credential metadata before rendering secret-safe provider settings without provider number writes, credential deletion, or live-test SMS helpers.

- Playwright starts the local Next.js test server on `127.0.0.1:3100` by default, separate from the normal dev port. Set `PLAYWRIGHT_PORT` to another integer port when a local process already uses that port. Existing-server reuse is opt-in with `PLAYWRIGHT_REUSE_EXISTING_SERVER=true`.
- `npm run test:e2e:demo` runs the investor demo path after the local database has been migrated and seeded.
- `npm run test:e2e:product-demo` runs the seeded product dashboard, contacts, campaign, campaign detail, inbox, template, analytics, and compliance paths for `/dashboard`, `/dashboard/contacts`, `/dashboard/campaigns`, `/dashboard/campaigns/:campaignId`, `/dashboard/inbox`, `/dashboard/templates`, `/dashboard/analytics`, and `/dashboard/compliance` after the local database has been migrated and seeded. The contacts path imports a local CSV row through `POST /api/contacts/imports`; the campaign path creates, preflights, schedules, edits a draft, and cancels a queued local campaign; the inbox path creates a local inbound reply, adds a note, and resolves/reopens a local thread; the template path creates reusable local copy; the analytics path reads existing local overview totals, including scheduled-campaign counts, without SMS, provider calls, billing, live AI, report execution, exports, mutations, or live messaging enablement.
- Contract drift tests verify the route scanner covers all supported Next API route methods, including `HEAD` and `OPTIONS`, so newly implemented handlers cannot bypass `contracts/CONTRACT-API.md` and `docs/API_MAP.md` documentation. The scanner ignores route export mentions inside comments, quoted strings, template literals, and regex literals, including escaped regex bodies and character classes, so examples cannot create false implemented-endpoint inventory entries.
- `npm run worker` processes local due scheduled campaign jobs through the dummy provider only and is blocked in production-like runtimes. Unit coverage verifies all production-like runtime markers (`NODE_ENV`, `VERCEL_ENV`, `DEPLOYMENT_ENV`, and `APP_ENV`) block before provider or future live-worker-class checks can fall through.
- `npm run worker:watch` runs the same worker in continuous polling mode for local demos. It is still blocked unless `MESSAGING_PROVIDER=dummy`, live messaging is disabled, and no production-like runtime marker is present.
- Worker readiness accepts only an unset `WORKER_DEPLOYMENT_CLASS` or `WORKER_DEPLOYMENT_CLASS=local-demo`; any future-looking production/live class is blocked before database or BullMQ workers can process scheduled campaign jobs, including the reserved planning-only `production-live-campaign` class. Worker provider readiness also accepts only unset, empty, or exactly `false` `LIVE_MESSAGING_ENABLED` values with the dummy provider; malformed live-messaging flag values fail closed.
- Live-worker control metadata tests verify that `production-live-campaign` stays outside the supported worker class list, that the future control checklist is runtime-frozen, and that the reserved class can be authorized only when the authorization wrapper is an ordinary object with no hidden required fields, inherited-field, or inherited-extra-field influence, exposes frozen data fields for `workerDeploymentClass` and `controls`, the supplied control array and entries are frozen, the entries are ordinary object records, the array exposes only enumerable indexed entries plus its native `length` in exact ordinary array key order, the `length` descriptor matches the exact checklist size, the exact frozen checklist IDs and requirement text are present in order, every control exposes only own enumerable data fields for the public `id`, `status`, and `requirement` fields, every control status is inside the supported vocabulary, and every listed control is explicitly implemented. Direct identity/status predicates also reject getter-backed public fields without executing those getters, hidden string, data-backed `Symbol.toStringTag`, custom iterator, data-backed custom iterator, custom async-iterator, own array method-name, or symbol control-array metadata, hidden string or symbol control-entry metadata, hidden required control fields, malformed primitive control entries, non-ordinary array/date/function-shaped control entries, inherited data-backed indexed slots, inherited accessor indexed slots, accessor-backed indexed slots, non-enumerable indexed control slots, writable/configurable indexed control slots, proxy-invalid indexed descriptors, sealed-but-writable, configurable public-field, duplicate-key, nullish public-field values, boxed string public-field values, case-drifted or whitespace-padded public strings including spaces, tabs, newlines, carriage returns, CRLF pairs, and invisible Unicode escape padding, or proxy-invalid public-field-descriptor control entries, and missing, accessor-backed, writable, proxy-invalid configurable, or proxy-invalid enumerable native length descriptors remain unauthorized, boxed numeric, mismatched or oversized safe-integer, malformed primitive, or hostile array `length` descriptors including boolean, bigint, symbol, string, and non-safe numeric values deny cleanly without coercing descriptor values, leaking descriptor invariant errors, or trusting indexed controls, malformed deployment-class primitive values including bigint, malformed object values including inherited-coercion-hook objects, and blank or whitespace-only deployment-class strings deny without coercion or supplied-control inspection, function-shaped controls evidence denies without invocation, object-shaped non-array controls evidence including proxy-backed maps, proxy-backed sets, proxy-backed weak collections, proxy-backed typed arrays, proxy-backed data views, proxy-backed array buffers, proxy-backed shared array buffers, proxy-backed promises, proxy-backed dates, proxy-backed regular expressions, proxy-backed errors, proxy-backed URLs, proxy-backed URLSearchParams, proxy-backed Web-platform records, proxy-backed weak references, and proxy-backed finalization registries, revoked proxy-backed built-in controls evidence including typed arrays, data views, weak collections, URL-shaped controls, and Web-platform records, hostile array-like getter or iterator shapes, proxy-backed or revoked proxy-backed array-prototype impostors, and `Symbol.toStringTag` array impostors denies without reading getters or traps, inherited array iterator hooks, transform/reducer method metadata, mutator/visitor method metadata, and copy-helper method metadata are not read while evaluating exact frozen control-array evidence, hidden string or symbol authorization-wrapper metadata remains unauthorized, own object-valued authorization-wrapper `constructor` or `toLocaleString` metadata denies without coercion or control inspection, own or inherited `Symbol.toStringTag` authorization-wrapper metadata denies before hostile controls are inspected and without reading tag getters, Date-shaped built-in authorization-wrapper impostors deny in ordinary, exact-field, proxy-backed, reflection-trapped, and revoked proxy-backed forms before control evidence is inspected, reordered or duplicate proxy keys on control arrays or authorization wrappers remain unauthorized, proxy-invalid authorization-wrapper field descriptors deny before controls are inspected, malformed proxy-backed or revoked-proxy-backed evidence denies cleanly without descriptor, key, prototype, or frozen-state reflection traps escaping on control arrays, control entries, or authorization wrappers, and descriptor-valid proxy-wrapped exact frozen evidence is evaluated through descriptors without executing array or entry `get` traps.
- Deployment-class metadata coverage proves object-shaped deployment-class values with own or inherited accessor-backed, own/inherited data-backed callable, own/inherited object-valued, or hidden own string/symbol metadata deny without reading metadata getters, invoking callable metadata or hostile coercion hooks, coercing hidden object-valued metadata, or inspecting supplied controls.
- Deployment-class coverage also proves proxy-backed and revoked proxy-backed Date evidence denies before supplied controls are inspected or object traps can authorize the reserved class.
- Live-worker authorization-wrapper coverage includes ArrayBuffer and, where runtime-supported, SharedArrayBuffer impostors in ordinary, exact-field frozen, proxy-backed, and revoked proxy-backed forms before supplied controls can be inspected.
- Live-worker authorization-wrapper coverage includes `URL` and `URLSearchParams` impostors in ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied controls can be inspected.
- Live-worker authorization-wrapper coverage includes runtime-supported Web-platform impostors such as `Blob`, `FormData`, `Headers`, `Request`, `Response`, stream objects, encoding-stream objects, event objects, `DOMException`, channel/port objects, queueing strategies, `URLPattern`, and `PerformanceObserver`, plus runtime-supported WebAssembly and Web Crypto records, in ordinary, exact-field frozen, proxy-backed, reflection-trapped, and revoked proxy-backed forms before supplied controls can be inspected. Controls-evidence coverage also includes focused runtime-supported WebAssembly and revoked proxy-backed Web Crypto records before the reserved class can authorize or inspect object traps.
- Live-worker authorization-wrapper coverage includes reflection-trapped built-in impostors before supplied controls can be inspected, without descriptor, key, prototype, frozen-state, or get traps escaping.
- Live-worker control metadata tests also prove hidden string metadata on otherwise valid control arrays, control entries, or authorization wrappers remains unauthorized before the reserved class can pass, including accessor-backed or data-backed hidden control-array metadata that denies without reading or invoking it, sealed-but-writable authorization wrappers deny before supplied controls are inspected, accessor-backed authorization-wrapper public fields deny without reading getters or inspecting supplied controls, accessor-backed and data-backed `Symbol.toStringTag` authorization-wrapper metadata and inherited accessor-backed deployment-class tag metadata deny without reading tag getters, invoking callable tag metadata, or inspecting supplied controls, own or inherited data-backed callable deployment-class metadata denies without callable invocation or supplied-control inspection, inherited `Object.prototype` authorization-wrapper `Symbol.toStringTag` metadata is not read while exact frozen wrapper evidence authorizes, that non-ordinary array/date/function-shaped authorization wrappers and proxy-backed or revoked proxy-backed null-prototype/class-instance authorization-wrapper impostors deny before supplied controls are inspected, and that collection including proxy-backed and revoked proxy-backed collection evidence, boxed primitive including proxy-backed and revoked proxy-backed boxed primitive evidence, array-buffer, runtime-supported typed-array including proxy-backed and revoked proxy-backed typed-array, URL-shaped, weak-reference, finalization-registry, runtime-supported Web-platform, WebAssembly, Web Crypto, and reflection-trapped proxy object deployment-class impostors deny before supplied controls are inspected.
- Live-worker control-entry metadata coverage proves own `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` hooks on otherwise matching control entries deny without reading, invoking, or coercing those hooks, including data-backed `Symbol.toStringTag`, accessor-backed coercion metadata, data-backed coercion metadata, and object-valued metadata with hostile coercion hooks. Live-worker control-array metadata coverage proves own `constructor`, own `toLocaleString`, own `Symbol.asyncIterator`, own data-backed `Symbol.toStringTag`, iterator, async-iterator, and coercion metadata, own object-valued tag, iterator, async-iterator, well-known symbol, array method-name, coercion, `constructor`, or `toLocaleString` metadata with coercion hooks, own accessor-backed and data-backed well-known symbol metadata, own array method-name metadata, `Symbol.toPrimitive`, `toString`, and `valueOf` hooks on otherwise matching control arrays deny without reading, invoking, or coercing those hooks, and exact frozen control-array evidence remains authorized without reading inherited `Array.prototype` coercion or `Symbol.toStringTag` metadata.
- Live-worker authorization-wrapper metadata coverage proves own object-valued `Symbol.toStringTag`, `Symbol.toPrimitive`, `toString`, and `valueOf` metadata with hostile coercion hooks denies before supplied controls are inspected and without coercing those metadata objects.
- Live-worker authorization-wrapper helper metadata coverage proves own object-valued iterator, well-known symbol, Object-helper, legacy accessor-helper, and `__proto__` metadata with hostile coercion hooks denies before supplied controls are inspected and without coercing those metadata objects.
- Live-worker control-array iterator, lookup, quantifier, transform/reducer, mutator/visitor, copy-helper, constructor, `toLocaleString`, Object-helper, legacy accessor-helper, prototype-accessor, and coercion coverage also proves exact frozen evidence remains authorized without reading or invoking inherited `Array.prototype` Object-helper metadata (`hasOwnProperty`, `propertyIsEnumerable`, or `isPrototypeOf`), inherited `Array.prototype` legacy accessor-helper metadata (`__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, or `__lookupSetter__`), inherited `Array.prototype.__proto__` prototype-accessor metadata, without reading or invoking inherited `Array.prototype.toLocaleString` metadata, and without invoking data-backed inherited index-slot metadata at occupied own checklist indexes or beyond the frozen checklist entries, non-index string or symbol metadata, `Array.prototype[Symbol.iterator]`, `Array.prototype[Symbol.asyncIterator]`, `Array.prototype[Symbol.toStringTag]`, `Array.prototype.constructor`, `Array.prototype[Symbol.unscopables]`, `Array.prototype[Symbol.isConcatSpreadable]`, string-method symbol metadata (`Symbol.match`, `Symbol.matchAll`, `Symbol.replace`, `Symbol.search`, or `Symbol.split`), iteration method metadata (`entries`, `keys`, or `values`), lookup method metadata (`at`, `includes`, `indexOf`, `lastIndexOf`, `find`, `findIndex`, `findLast`, or `findLastIndex`), quantifier metadata (`every`), transform/reducer method metadata (`filter`, `flatMap`, `map`, `reduce`, or `reduceRight`), mutator/visitor method metadata (`concat`, `copyWithin`, `fill`, `flat`, `forEach`, `join`, `pop`, `push`, `reverse`, `shift`, `slice`, `some`, `sort`, `splice`, or `unshift`), copy-helper method metadata (`toReversed`, `toSorted`, `toSpliced`, or `with`), `Array.prototype[Symbol.toPrimitive]`, `Array.prototype.toString`, or `Array.prototype.valueOf` metadata.
- Live-worker control metadata tests also prove proxy-backed non-array `controls` evidence denies without reading object `get`, prototype, descriptor, or key traps before the reserved class can pass.
- `WORKER_MAX_JOBS_PER_POLL` caps local worker throughput per poll and is clamped to a bounded range for demos/tests.
- BullMQ enqueue and worker helpers are unit-tested without requiring Redis. BullMQ worker readiness coverage also verifies all production-like runtime markers block before provider or future live-worker-class checks can fall through. Local validation must remain green with `QUEUE_BACKEND` unset or set to `database`.
- Production deployment gate tests verify that production-like environments cannot enable external-impact settings without an explicit future override.
- API rate limiting helpers are unit-tested with deterministic clocks and isolated stores. Local validation keeps the middleware defaults generous enough for smoke and demo paths.
- Campaign create/update route tests verify malformed JSON returns `400` before local campaign create/update repository mutations run. Contact create/update/merge/import route tests verify malformed JSON returns `400` before local contact upsert/update/merge, CSV parsing, or import repository mutations run. Inbox/demo inbound route tests verify malformed JSON returns `400` before local inbound message, conversation message, assignment, note, or resolve repository mutations run. Fake AI route tests verify malformed JSON returns `400` before conversation lookup, fake provider execution, or local `AI_REQUEST` usage metering can run. Billing usage, campaign preflight, compliance settings, provider number metadata, provider settings, and live-test SMS route tests verify malformed JSON returns `400` before local usage records, local preflight repository work, compliance updates, readiness audit writes, provider metadata writes, provider settings rendering, or gated live-test send helpers can run. Readiness audit route tests verify unsupported JSON/CSV export query filters return `400` before local audit event reads or CSV serialization, and supported bounded filters read only local audit metadata. Provider credential rotation route tests verify unsupported JSON/CSV export query filters return `400` before local rotation-history reads or CSV serialization, and supported bounded filters read only local redacted credential metadata. Compliance settings route tests verify role denials return before request-body parsing, and successful admin requests update only local compliance metadata and local readiness audit records before evaluating the local messaging hard gate without provider metadata writes or live-test SMS helpers. Provider number metadata route tests verify role denials return before request-body parsing, and successful admin requests upsert only local provider number metadata without provider settings rendering, provider credential persistence, or live-test send helpers. Provider settings delete route tests verify role denials return before local credential metadata is cleared, and successful deletion clears only local metadata before rendering secret-safe provider settings. Campaign schedule route tests verify role denials return before request-body parsing, malformed or schema-invalid schedule payloads return `400` before local queue scheduling or BullMQ enqueue helpers can run, missing campaigns return `404` without BullMQ enqueue, and valid payloads persist a local queue job before optional BullMQ enqueue.
- Live-worker control tests verify exact frozen control-entry evidence remains descriptor-based and does not read accessor-backed or invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen authorization-wrapper evidence remains descriptor-based and does not read accessor-backed or invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen live-worker evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` `Symbol.toPrimitive`, `toString`, or `valueOf` metadata while evaluating the still-reserved `production-live-campaign` authorization path.
- Live-worker control tests verify exact frozen authorization-wrapper evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` public-field metadata for `workerDeploymentClass` or `controls`, including when a wrapper is missing own `controls`.
- Live-worker control tests verify exact frozen control-entry evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype` public-field metadata for `id`, `status`, or `requirement`.
- Live-worker control tests verify exact frozen authorization-wrapper and control-entry evidence remains descriptor-based and does not invoke data-backed inherited `Object.prototype[Symbol.toStringTag]` metadata.
- Live-worker control tests verify exact frozen control-array density, descriptor-enumerability, scratch-array, and frozen evidence checks remain descriptor-based and do not read or invoke inherited `Object.prototype[Symbol.iterator]`, `Object.prototype.toLocaleString`, arbitrary inherited `Object.prototype` non-public string or symbol metadata, `Object.prototype.hasOwnProperty`, `Object.prototype.propertyIsEnumerable`, `Object.prototype.isPrototypeOf`, `Object.prototype.constructor`, `Object.prototype.__proto__` prototype-accessor metadata, or legacy accessor-helper metadata (`__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, or `__lookupSetter__`).
- API operations inventory tests verify that the read-only `/settings/api` route list includes implemented local methods such as contact soft archive, campaign draft update, inbox message/note reads, billing usage reads, and the gated live test SMS endpoint while keeping route-method rows unique, backing `app/**/route.ts` files present, reverse coverage for implemented API methods, and live test SMS as the only external-impact route with explicit Twilio allowlist-gate safety copy. The seeded investor demo path verifies that the `POST /api/demo/live-test-sms` row visibly carries the same external-impact classification and safety copy.
- Webhook helper tests verify string-only and duplicate-free form payload enforcement before signature validation, Twilio signature validation includes unknown provider fields, plus inbound/status normalization, including provider retry whitespace/casing drift, blank modern/legacy alias fallback, inbound body preservation, address trimming, idempotency-key stability, and terminal delivered versus failed/undelivered timestamp cleanup.
- API operations inventory tests verify that exported route metadata keeps supported methods and no-impact summary states from runtime-frozen vocabularies, derives implemented-route reverse coverage from the exported method vocabulary, canonical local `/api/` path shape, boolean flags, nonblank public copy, frozen route snapshots, stable route order, and public route/status fields so malformed values, caller-side mutations, accidental order churn, or accidental extra fields cannot drift later `/settings/api` renders.
- Mutating API authorization tests verify that route-handler discovery ignores `POST`, `PATCH`, `PUT`, and `DELETE` export mentions inside comments, quoted strings, and non-executable template literals while still tracking real named-export handlers.
- Mutating API authorization tests verify that `Request.prototype` body readers invoked with a request or clone target, including direct `.call`, `.apply`, `.bind`, bracketed `Request["prototype"]` access, direct `Request` constructor aliases, direct/assigned/destructured `Request.prototype` object aliases, const-asserted bracketed prototype names, `globalThis.Request.prototype` access, optional/bracketed `globalThis["Request"]` access, const-asserted `globalThis["Request" as const]` access, local aliases, parenthesized local `globalThis` aliases, and computed property-name aliases of `globalThis.Request`, `Reflect.apply` aliases, and optional or bracket-notation `Reflect.apply` forms such as `Reflect?.apply(...)`, `Reflect.apply?.(...)`, and `Reflect["apply"](...)`, are treated as body parsing and must remain after each handler's own top-level `requireApiRole` call.
- Mutating API authorization tests verify that parenthesized, nested-parenthesized, TypeScript type-asserted, TypeScript `satisfies`, and TypeScript non-null `Request` constructor, `globalThis.Request` constructor, bracketed `globalThis["Request"]` constructor, local `globalThis` alias constructor, parenthesized type-asserted `globalThis` root constructor, whole-parenthesized type-asserted `globalThis` root constructor, and `Request.prototype` aliases, such as `const RequestCtor = ((Request))`, `const RequestCtor = Request as typeof Request`, `RequestCtor = Request as typeof Request`, `const RequestCtor = Request satisfies typeof Request`, `RequestCtor = Request satisfies typeof Request`, `const RequestCtor = Request!`, `RequestCtor = Request!`, `const RequestCtor = ((globalThis.Request))`, `const RequestCtor = globalThis.Request as typeof Request`, `const RequestCtor = (globalThis as typeof globalThis).Request`, `const RequestCtor = ((globalThis as typeof globalThis).Request)`, `const RequestCtor = globalThis.Request!`, `RequestCtor = globalThis.Request!`, `const RequestCtor = ((globalThis["Request"]))`, `const root = globalThis; const RequestCtor = ((root)).Request`, `const requestPrototype = ((Request)).prototype`, `const requestPrototype = (Request.prototype)`, `const requestPrototype = Request.prototype as typeof Request.prototype`, `requestPrototype = Request.prototype as typeof Request.prototype`, `const requestPrototype = Request.prototype satisfies typeof Request.prototype`, `requestPrototype = Request.prototype satisfies typeof Request.prototype`, `const requestPrototype = Request.prototype!`, and `requestPrototype = Request.prototype!`, are normalized before role-gate ordering checks.
- Mutating API authorization tests verify that standard request body-reader invocations through bracket-notation call/apply/bind methods, such as `req.json["call"](req)`, ``req.clone().text[`apply`](req.clone())``, `req.formData["bind"](req)()`, and bound-reader alias calls such as `readBlob["call"](undefined)`, are normalized before role-gate ordering checks.
- Mutating API authorization tests verify that direct request body-reader property aliases, such as `const readerName = "json"; await req[readerName]()` plus request aliases, assigned reader-name aliases, optional readers, and cloned request variants, are normalized before role-gate ordering checks.
- Mutating API authorization tests verify that TypeScript type-asserted, `satisfies`, and non-null request body-reader expressions, such as `(req.text as typeof req.text).call(req)`, `const readFormData = req.formData satisfies typeof req.formData`, `const readBlob = cloned.blob as typeof cloned.blob`, `req.json!()`, and `req.clone().text!.call(req.clone())`, are normalized before role-gate ordering checks.
- Mutating API authorization tests verify descriptor-derived `Request.prototype`, `Object.getPrototypeOf(request)`, and `Reflect.getPrototypeOf(request)` body readers accessed through `Object.getOwnPropertyDescriptor` or `Reflect.getOwnPropertyDescriptor` with `.value` or bracket `["value"]` forms are treated as body parsing and must remain after each handler's own top-level `requireApiRole` call, including optional and bracket-notation descriptor/prototype lookup forms, local descriptor object aliases before `.value` access, destructured `value` and `["value"]` reader aliases, parenthesized or const-asserted literal descriptor property names, and local, destructured, computed destructured, or computed member aliases for descriptor/prototype lookup functions.
- Mutating API authorization tests verify optional and bracket-notation `Reflect.get` standard request body-reader lookups such as `Reflect?.get(...)`, `Reflect.get?.(...)`, and `Reflect["get"](...)` are normalized before role-gate ordering checks, including local parenthesized and TypeScript const-asserted body-reader property aliases such as `const readerName = ("json")` or `const readerName = "json" as const`, plus local direct, assigned, destructured, computed destructured, and computed member aliases for `Reflect.get` and `Reflect.apply`, so reflective body parsing still has to stay after each handler's own top-level `requireApiRole` call.
- Mutating API authorization tests verify direct or aliased reflective helper invocations through `.call` or `.apply`, including `Reflect.get`, `Reflect.apply`, descriptor lookups, and prototype lookups, are normalized before role-gate ordering checks when they resolve standard request body readers.
- Mutating API authorization tests verify that `globalThis.Object` and `globalThis.Reflect` access paths are normalized before reflective body-reader checks, including dot, optional-dot, bracket, computed string-alias bracket access, bracketed built-in method calls, local `globalThis` aliases, TypeScript type-asserted, parenthesized TypeScript type-asserted, declaration and assigned TypeScript `satisfies` local `globalThis` aliases, parenthesized `satisfies` local `globalThis` aliases, computed built-in access through local `globalThis` aliases, direct and assigned aliases from `globalThis.Object`, `globalThis.Reflect`, bracketed `globalThis["Object"]`, bracketed `globalThis["Reflect"]`, parenthesized and whole-parenthesized type-asserted or `satisfies` `globalThis` root member aliases such as `const ReflectBuiltin = (globalThis as typeof globalThis).Reflect`, `const ReflectBuiltin = ((globalThis as typeof globalThis).Reflect)`, and `const ReflectBuiltin = ((globalThis satisfies typeof globalThis).Reflect)`, and computed local-`globalThis` built-in names, destructured `globalThis` built-in aliases, type-asserted and `satisfies` destructured `globalThis` `Object`/`Reflect`/`Request` aliases, computed destructured `globalThis` built-in aliases, parenthesized, and nested-parenthesized access to those built-ins. Parenthesized and nested-parenthesized direct `Object` and `Reflect` built-in access paths, direct and assigned local built-in aliases, TypeScript type-asserted built-in aliases, parenthesized TypeScript type-asserted built-in aliases, and declaration and assigned TypeScript `satisfies` built-in aliases are normalized before the same checks.
- Mutating API authorization tests verify computed destructured standard request body-reader aliases such as `const { ["json"]: readJson } = request` and `const { [readerName]: readText } = request.clone()` are normalized before role-gate ordering checks, so destructured body parsing still has to stay after each handler's own top-level `requireApiRole` call.
- Contract operations inventory tests verify that `/settings/contracts` uses static validated metadata for required contract paths, validation commands backed by `package.json` scripts, runtime-frozen supported validation-command and no-impact vocabularies, drift controls, public fields, frozen snapshots, detached returned arrays with aligned counts, stable order, unique identifiers, secret-like literal rejection, and read-only counts without reading contract file contents or executing validation commands.
- Validation operations inventory tests verify that `/settings/validation` uses static validated metadata for gate commands backed by `package.json` scripts, runtime-frozen supported gate-command and local-only area vocabularies, repair signals, public fields, frozen snapshots, detached returned arrays with aligned counts, stable order, unique identifiers, required boundary terms, secret-like literal rejection, no command execution, no external impact, and no secret display without running checks or inspecting logs.
- Security operations inventory tests verify that `/settings/security` static metadata keeps command references inside a runtime-frozen supported command vocabulary, keeps those commands backed by `package.json` scripts, exports runtime-frozen local-only status and no-impact vocabularies, keeps returned arrays detached with aligned counts, and rejects secret-like literals plus command-like literals outside the allowlisted validation-command field before rendering local security copy.
- Webhook helper tests verify delivery-status casing/whitespace plus provider message ID and error-code whitespace are normalized before webhook idempotency keys are derived, preventing duplicate local webhook events from provider retry formatting drift. They also verify blank `MessageSid`/`MessageStatus` values fall back to nonblank legacy `SmsSid`/`SmsStatus` aliases, and inbound webhook bodies reject whitespace-only payloads while preserving nonblank body text exactly for local message creation.
- Notification operations inventory tests verify that `/settings/notifications` uses static validated metadata for supported local channel names, channel boundaries, runtime-frozen vocabularies with caller-mutation rejection, detached returned arrays with aligned counts, no-impact summary states for command execution, external impact, mutation, and secret display, no-send controls, safety-boundary copy, public fields, frozen snapshots, stable order, unique identifiers, no command execution, no external impact, no mutation, no secret display, required no-send control terms, command-like literal rejection, and secret-like literal rejection before rendering local notification copy.
- Queue operations inventory tests verify that `/settings/queue` uses static validated metadata for supported local worker command references, runtime-frozen worker command and worker mode vocabularies with caller-mutation rejection, safety-boundary copy, public fields, frozen snapshots, stable order, unique identifiers, package-script references, no command execution, no external impact, no secret display, and secret-like literal rejection before rendering local queue copy.
- Readiness audit operations inventory tests verify that `/settings/readiness-audit` uses static validated metadata for allowlisted audit actions, subject types, runtime-frozen vocabularies with caller-mutation rejection, bounded positive default-limit and export-limit vocabularies, default query limit derivation, maximum query limit-ceiling derivation, centralized CSV export-link construction, command-execution state, no-impact summary states, safety-boundary copy, public fields, frozen snapshots, stable order, unique identifiers, command-like literal rejection, and secret-like literal rejection before rendering local readiness history filters or CSV links.
- Operations index inventory tests verify grouped local operator routes, duplicate prevention, safety-sensitive links, and backing `app/**/page.tsx` files before browser demo coverage.
- Operator runbook inventory tests verify that the runbook admin-link list is projected from the shared local operator surface inventory, keeps labels aligned, excludes only non-settings surfaces, and points every listed route at an implemented `app/**/page.tsx`.
- Operations index inventory tests verify both directions of page coverage: every listed route has an implemented `app/**/page.tsx`, and every implemented local operator page is listed in the shared inventory.
- Demo operations inventory tests verify that seeded readiness checkpoint signals and operational links are projected from the shared local operator surface inventory and point at implemented app pages.
- Reporting, workflow, and release inventory tests verify that report links, workflow checkpoint owners, and release surface links are projected from the shared local operator surface inventory and point at implemented app pages.
- The seeded investor demo path verifies `/settings/demo` visible checkpoint names, shared signal labels, boundaries, operational link labels, and link targets from the same shared operations inventory without imports, scheduling, worker execution, inbox replies, report execution, exports, mutations, provider calls, billing, notifications, live AI, SMS, email, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/reports`, `/settings/workflows`, and `/settings/releases` visible links, route targets, labels, notes, and workflow boundaries from the same shared operations inventory without command execution, report execution, workflow execution, exports, mutations, provider calls, billing, notifications, live AI, SMS, email, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/operations` as a read-only grouped local operator surface index and checks visible links/routes from the shared operations inventory without commands, file inspection, API probes, mutations, exports, provider calls, billing, notifications, live AI, SMS, email, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/compliance` as a read-only compliance detail surface without live provider verification or messaging enablement.
- The seeded investor demo path verifies `/settings/usage` as a read-only local usage and analytics surface without live billing or provider configuration.
- The seeded investor demo path verifies `/settings/numbers` as a read-only local provider-number metadata surface without provisioning, provider calls, or messaging enablement.
- The seeded investor demo path verifies `/settings/campaigns` as a read-only campaign and queue operations surface without scheduling campaigns, running workers, provider calls, billing, notifications, queue mutation, or messaging enablement.
- The seeded investor demo path verifies `/settings/queue` as a read-only scheduled queue operations surface without enqueueing jobs, running workers, Redis calls, provider calls, billing, notifications, queue mutation, campaign updates, or messaging enablement.
- The seeded investor demo path verifies `/settings/queue` no-impact summary labels for command execution, external impact, and secrets displayed without executing workers, enqueueing jobs, Redis calls, provider calls, billing, notifications, SMS, mutations, campaign updates, or live messaging enablement.
- The seeded investor demo path verifies `/settings/contacts` as a read-only contact operations surface without importing contacts, mutating consent, changing labels, provider calls, billing, notifications, or messaging enablement.
- The seeded investor demo path verifies `/settings/data` as a read-only data operations surface without hard deletion, record mutation, provider calls, billing, notifications, live AI, SMS, or live feature enablement.
- The seeded investor demo path verifies `/settings/audience` as a read-only audience operations surface without changing memberships, evaluating segments for sends, provider calls, billing, notifications, label mutation, or messaging enablement.
- The seeded investor demo path verifies `/settings/templates` as a read-only template operations surface without creating templates, editing copy, rendering live outbound messages, scheduling campaigns, provider calls, billing, notifications, or messaging enablement.
- The seeded investor demo path verifies `/settings/inbox` as a read-only shared inbox operations surface without message creation, assignment, resolution, provider calls, billing, notifications, contact mutation, or messaging enablement.
- The seeded investor demo path verifies `/settings/delivery` as a read-only delivery operations surface without sends, retries, webhook replays, message mutation, provider calls, billing, notifications, SMS, or messaging enablement.
- Unit coverage pins the static delivery operations inventory before `/settings/delivery` renders it, including checkpoint status vocabularies, no-impact summary states, frozen snapshots, detached returned arrays, stable order, whitespace-clean copy, secret-like and command-like literal rejection, and required no-send/no-mutation boundary terms.
- The seeded investor demo path verifies `/settings/team` as a read-only team operations surface without invites, role changes, suspensions, membership deletion, Clerk calls, email, notifications, provider calls, billing, SMS, or messaging enablement.
- The seeded investor demo path verifies `/settings/billing` as a read-only local billing operations surface without Stripe calls, subscriptions, invoices, payment collection, card charges, email, notifications, provider calls, SMS, or live billing enablement.
- The seeded investor demo path verifies `/settings/reports` as a read-only local reporting index with report links, operational metrics, readiness signals, and safety-boundary text without executing reports, creating exports, mutating records, provider calls, billing, notifications, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/integrations` as a read-only integration operations surface with provider, AI, billing, webhook, and notification boundaries without provider calls, prompt submission, billing artifacts, notifications, mutations, secret exposure, enqueueing, exports, or live feature enablement.
- Unit and seeded browser coverage verify that `/settings/integrations` integration surface links and `/settings/security` navigation links are projected from the shared local operator surface inventory, including visible labels, route targets, states, and boundaries without executing commands, calling providers, billing, notifying, exposing secrets, sending SMS or email, or enabling live features.
- Unit and seeded browser coverage verify that `/settings/exports` admin navigation links are projected from the shared local operator surface inventory, including visible labels, route targets, notes, and backing pages without creating exports, mutating records, calling providers, billing, notifying, exposing secrets, sending SMS or email, or enabling live features.
- Unit and seeded browser coverage verify that `/settings/webhooks`, `/settings/delivery`, and `/settings/team` header navigation links are projected from the shared local operator surface inventory, including visible labels, route targets, and backing pages without replaying webhooks, retrying deliveries, inviting users, mutating records, calling providers, billing, notifying, sending SMS or email, exposing secrets, or enabling live features.
- Unit and seeded browser coverage verify that `/settings/provider`, `/settings/numbers`, `/settings/compliance`, `/settings/system`, `/settings/usage`, and `/settings/readiness-audit` header navigation links are projected from the shared local operator surface inventory, including visible labels, route targets, and backing pages without calling providers, provisioning numbers, mutating compliance/audit records, billing, notifying, sending SMS or email, exposing secrets, or enabling live features.
- Unit coverage verifies that every shared per-page operator navigation projection has unique route entries, points at implemented app pages, and resolves only through the shared local operator surface inventory.
- Unit coverage verifies that every shared operator navigation projection plus rich demo/workflow/integration projection labels and notes derive visible copy from the supplied inventory instance, so helper overrides cannot silently fall back to stale global labels or notes.
- Unit coverage verifies that broad shared operator inventory projections honor supplied inventory omissions, so launch, settings, runbook, demo-console, and summary helpers cannot reintroduce stale global routes.
- Unit coverage verifies that shared operator inventory projection helpers leave supplied inventory groups and links unchanged while deriving navigation, rich checkpoints, workflow steps, and integration areas.
- Unit coverage verifies that shared operator projection helpers return fresh and frozen result arrays per call, so consumer-side array changes cannot leak into later navigation projections.
- Unit coverage verifies that every shared operator projection result object is frozen across navigation, demo checkpoint, workflow, and integration projections, so no later entry can be mutated by callers.
- Unit coverage verifies that the canonical shared operator surface inventory is validated before export and frozen at runtime, including the exported group array, nested link arrays, and link objects, so malformed built-in route groups or accidental mutation fail before local navigation projections drift.
- Unit coverage verifies that shared operator projection links are detached objects from supplied inventory links while preserving the shared href, label, and note copy.
- Unit coverage verifies that the shared operator surface summary exposes only public aggregate fields, so extra supplied inventory group or link properties cannot leak into local operation counts or route lists.
- Unit coverage verifies that the shared operator surface summary returns a fresh frozen routes array per call, so caller-side route array changes cannot leak into later local operation counts or route lists.
- Unit coverage verifies that supplied shared operator inventories with duplicate route entries fail before summaries or projections are derived, so local navigation helpers cannot silently shadow a route.
- Unit coverage verifies that supplied shared operator inventories with empty groups fail before summaries or projections are derived, so local navigation helpers cannot render group headings with no reachable local surface.
- Unit coverage verifies that supplied shared operator inventories with duplicate group names, link labels, or link notes fail before summaries or projections are derived, so local navigation helpers cannot silently render ambiguous copy.
- Unit coverage verifies that supplied shared operator inventories that are not arrays fail before summaries or projections are derived, so malformed runtime inventory values cannot surface generic errors.
- Unit coverage verifies that supplied shared operator inventory arrays with custom prototypes or extra string/symbol fields fail before summaries or projections are derived, so local navigation helpers only accept plain inventory arrays.
- Unit coverage verifies that empty supplied shared operator inventories fail before summaries or projections are derived, so blank local navigation surfaces cannot be treated as valid.
- Unit coverage verifies that supplied shared operator inventories with blank group names, routes, labels, or notes fail before summaries or projections are derived, so whitespace-only local navigation fields cannot be treated as valid.
- Unit coverage verifies that supplied shared operator inventories with invalid group objects fail before summaries or projections are derived, so malformed runtime inventory entries cannot surface generic errors.
- Unit coverage verifies that supplied shared operator inventories with sparse group entries fail as missing inventory array descriptors before summaries or projections are derived, so malformed runtime group slots cannot be silently skipped or read.
- Unit coverage verifies that supplied shared operator inventories with invalid link objects fail before summaries or projections are derived, so malformed runtime link entries cannot surface generic errors.
- Unit coverage verifies that supplied shared operator inventories with non-string group names, routes, labels, or notes fail before summaries or projections are derived, so malformed runtime field values cannot surface generic string-operation errors.
- Unit coverage verifies that supplied shared operator inventories with non-local, uppercase, query, hash, trailing-slash, double-slash, or dynamic-segment routes fail before summaries or projections are derived, so malformed runtime routes cannot enter local navigation helpers.
- Unit coverage verifies that supplied shared operator inventories with accessor-backed group or link fields fail before summaries or projections are derived, so local navigation helpers only read plain data fields.
- Unit coverage verifies that supplied shared operator inventories with custom-prototype group or link records fail before summaries or projections are derived, so local navigation helpers only accept ordinary object records.
- Unit coverage verifies that supplied shared operator inventories with extra string or symbol fields on groups or links fail before summaries or projections are derived, so local navigation helpers only accept exact public record shapes.
- Unit coverage verifies that supplied shared operator inventory link arrays with custom prototypes or extra string/symbol fields fail before summaries or projections are derived, so local navigation helpers only accept plain link arrays.
- Unit coverage verifies that supplied shared operator inventory arrays and group link arrays with sparse, accessor-backed, or hidden index slots fail as descriptor errors before summaries or projections read local navigation entries.
- Unit coverage verifies that shared operator projections fail loudly with the missing route when a projection references a route absent from the supplied inventory.
- Unit coverage verifies that shared operator surface labels and notes remain aligned with their route names, including singular/plural route segment variants.
- Unit coverage verifies that rich shared operator projections for demo checkpoints, workflow steps, and integration areas have unique route entries, point at implemented app pages, and derive their visible labels from the shared local operator surface inventory.
- Unit coverage verifies that rich shared operator projection names, labels, states, and boundaries stay unique, whitespace-clean, and explicit about read-only/no-impact boundaries.
- Unit coverage verifies that rich shared operator projection boundaries name external-impact exclusions such as provider calls, SMS, billing, mutations, exports, queue activity, or paid AI before those boundaries are rendered in local operator surfaces.
- Unit coverage verifies that page-specific operator navigation projections exclude their own current route, while broader inventory views such as the operator runbook may intentionally list all local admin pages.
- Unit coverage verifies that every shared operator surface route stays in canonical static app-page form: lowercase, no trailing slash, no query/hash, no dynamic segment, no double slash, and limited to `/demo`, `/settings`, or `/settings/**`.
- Unit coverage verifies that shared operator surface routes, group names, labels, and notes stay whitespace-clean without leading/trailing whitespace, doubled spaces, or embedded newlines.
- Unit coverage verifies that shared operator surface group names and labels stay in stable Title Case navigation format, and notes stay short lower-case sentence fragments without terminal punctuation.
- Unit coverage verifies that shared operator surface group names, labels, and notes stay concise for operator scanning, and labels keep predictable navigation suffixes.
- Unit coverage verifies that shared operator surface group names, labels, and notes stay action-neutral, avoiding command-style copy such as send, run, delete, enable, replay, or mutate.
- Unit coverage verifies that shared operator surface group and route order stay stable so projected navigation does not churn without an intentional inventory update.
- The seeded investor demo path verifies `/settings/workflows` as a read-only workflow operations surface with demo path checkpoints, runtime boundaries, and operational signals without imports, scheduling, worker execution, inbox replies, delivery retries, prompt submission, report execution, exports, mutations, provider calls, billing, notifications, or live feature enablement.
- The seeded investor demo path verifies `/settings/releases` as a read-only release operations surface with checklist commands, runtime boundary, release surface links, and safety-boundary text without command execution, migrations, tests, browser launches, git operations, deploys, mutations, provider calls, billing, notifications, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/health` as a read-only health operations surface with the local health endpoint contract, demo-safe defaults, runtime boundary, operational links, and safety-boundary text without health probes, API calls, commands, mutations, provider calls, billing, notifications, secrets, or live feature enablement.
- The seeded investor demo path verifies `/settings/notifications` as a read-only notification no-send boundary without recipients, templates, jobs, sends, alerts, webhooks, provider calls, billing records, live AI calls, SMS, email, mutations, or notification delivery.
- The seeded investor demo path verifies `/settings/readiness-audit` as a read-only local readiness audit surface with filters, bounded CSV export links, and no-impact summary labels for command execution, external impact, mutation, and secrets displayed without mutating audit events, exposing secrets, calling providers, billing, live AI, SMS, email, notifications, or enabling live features.
- The Playwright smoke test verifies the root local launch dashboard links from the shared local operator surface inventory while still rendering demo-safe defaults, so browser coverage follows the same route source as the page.
- The seeded product-demo path verifies `/dashboard` renders the owner-facing product navigation, seeded metrics, opt-in rate, consent/compliance readiness, campaign, inbox, template, analytics, fake-AI usage visibility, and demo-safe live-blocked states without provider calls, billing records, notifications, live AI, SMS, email, secrets, or live feature enablement. It also verifies `/dashboard/contacts`, `/dashboard/campaigns`, `/dashboard/campaigns/:campaignId`, `/dashboard/inbox`, `/dashboard/templates`, `/dashboard/analytics`, and `/dashboard/compliance` use existing local APIs without provider calls, billing, live AI, SMS, secrets, hard deletion, report execution, exports, worker execution, notifications, or live feature enablement.
- The seeded investor demo path verifies `/settings/runbook` as a read-only local operator checklist with links to current local admin surfaces, including queue, delivery, readiness audit, and provider numbers, without command execution or external-impact side effects.
- Campaign cancel route unit coverage verifies role denials return before local cancellation, missing campaigns return `404`, existing non-scheduled campaigns return `409`, and successful cancellation returns the locally paused campaign without provider calls, BullMQ enqueueing, worker execution, billing, notifications, SMS, or live feature enablement. Repository coverage verifies only scheduled campaigns can be locally paused by cancellation; missing campaigns return without queue mutations, while draft, paused, and completed campaigns reject without queue or campaign mutations.
- Template create/update route unit coverage verifies malformed JSON returns `400` before local template upsert/update repository mutations run.
