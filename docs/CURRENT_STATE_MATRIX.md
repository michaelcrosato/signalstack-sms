# Current State Matrix

Last updated: 2026-05-24.

This file is intentionally compact. Historical run notes are preserved in `LOOP_LOG.md` and `docs/LOOP_LOG.md`; current agents should use this matrix plus targeted searches instead of loading old run history.

Latest validated run: Run 796 adds a checked production auth/RBAC planning boundary and blocks Clerk auth configuration in production-like demo deployments.

| Area | Backend/API State | Browser State | Main Gap | Next Action |
| --- | --- | --- | --- | --- |
| Contacts | Tenant-scoped contacts, consent fields, CSV import, tags/lists/segments foundations, archive/restore/merge support. | Dashboard contacts list/import/detail workflows and product demo coverage. | Merge is local-only and intentionally preserves source rows through soft archive. | Keep merge local-only; revisit conflict review after product demo feedback. |
| Campaigns | Drafts, recipients, preflight with missing/cross-tenant requested contact IDs blocked as `CONTACT_NOT_FOUND`, schedule/cancel records, scheduled-only local cancellation, queue jobs, dummy worker path, and campaign-linked local message rows. | Campaign composer, fake-AI copy assist, recipient selection, visible allowed/blocked preflight counts, local schedule, detail edit/cancel, campaign-list recipient readiness/blocker reporting, delivered/pending/failed/rate reporting, recipient send-state/block-reason visibility, dashboard next-step/outbound-delivery rate/pending/failure signals, local outbound-only mutually exclusive delivered/pending/failed delivery snapshot and row states, centralized dashboard/analytics outbound-delivery count filters, and product demo coverage. | No advanced campaign history beyond summary counts and recent local delivery/message metadata. | Keep live sends blocked; add deeper reporting only after product demo feedback. |
| Inbox | Conversations, messages, assignment, notes, resolve/reopen, demo inbound, early explicit inbound idempotency duplicate detection, STOP/HELP parsing, fake-AI summary/lead qualification endpoints. | Inbox list/thread workflow with `conversationId` query selection and first-visible fallback, local demo inbound, fake-AI insights, notes, assignment, resolve/reopen. | No outbound reply send path. | Keep inbox local-only until live/provider gates mature. |
| Templates | Template model and tenant-scoped create/read/update APIs. | Template create/list/detail/edit workflows. | No delete/archive workflow. | Revisit lifecycle controls after product demo feedback. |
| Compliance | Profile/checklist APIs, readiness audit, opt-out foundations, central messaging gates. | Compliance readiness detail and operations pages. | A2P, quiet-hours, consent evidence, and live-send policy need production implementation. | Keep live campaign sends blocked; revisit production compliance depth after demo feedback. |
| Twilio/Provider | Webhook foundations with controlled malformed-form rejection before signature/tenant/local mutations, route coverage for invalid signatures, duplicate no-op behavior, payload validation, inbound/status normalization, tenant-scoped duplicate-race handling, tenant-scoped status updates, provider metadata, credential readiness, gated live-test SMS with normalized local provider-status evidence. | Provider operations page plus `/demo` live-test SMS. | Full live campaign sending is intentionally not enabled. | Keep isolated test SMS; harden Twilio adapter behind explicit gates later. |
| AI | Deterministic fake provider and local AI endpoints that record local `AI_REQUEST` usage after successful fake output. | Campaign copy assist and inbox summary/lead qualification use local endpoints. | No live AI provider or cost controls. | Keep fake AI for demo; defer live AI until cost/data gates exist. |
| Billing | Local usage events, billing account data, usage APIs, and fake-AI usage metering. | Billing/usage operations pages. | No Stripe, payments, invoices, or subscription lifecycle. | Defer until Phase 3 behind live billing gates. |
| Auth/RBAC | Demo current user, role helpers, tenant helpers, broad mutating-route role enforcement coverage, checked production auth/RBAC plan, and production-gate blocking for Clerk auth configuration in production-like demo deployments. | Demo-safe single-user experience plus read-only team operations. | No production auth provider; Clerk-backed auth remains blocked until session verification, active membership enforcement, and route RBAC controls exist. | Keep production auth planning executable; add real auth/RBAC after product flow stabilizes. |
| Queue/Worker | Durable jobs, tenant-scoped idempotency, schedule-time stale same-campaign queued-job cancellation, dummy worker with tenant-explicit outbound message/provider idempotency keys, stale schedule skips, per-recipient send-time consent/archive skips, local outbound provider-status preservation, all-marker production-like runtime blocking, optional BullMQ/Redis mirror, `WORKER_DEPLOYMENT_CLASS=local-demo`, and extensive blocked `production-live-campaign` control evidence checks. | Queue operations page. | Worker execution remains local/demo-only; no production/live worker deployment class is authorized. | Keep live sends blocked; implement every named production control before live worker support. |
| Rate Limiting | Local in-memory API middleware. | Visible in operations status. | Not production-distributed. | Move to Redis or platform-backed limiter when production infrastructure is configured. |
| Operations Surfaces | Extensive inventory-backed read-only pages and tests. | Many `/settings/**` pages; `/settings/api` marks route-level external-impact classification, with only `POST /api/demo/live-test-sms` marked external impact. | Operations UI has outgrown product UI. | Stop expanding operations pages unless they support release safety. |
| Tests/Gates | `npm run validate`, protected `.\scripts\local-gate.ps1`, unit tests including direct Twilio webhook route fail-closed/duplicate/tenant-scope coverage, webhook malformed-form helper rejection and duplicate create-race handling, explicit inbox idempotency side-effect prevention and inbox query-selection fallback, campaign preflight missing/cross-tenant contact blocking, campaign stale-queue cancellation, worker stale-schedule skip coverage, tenant-explicit worker outbound idempotency keys, worker partial-recipient send-time skip and provider-status persistence coverage, gated live-test SMS status normalization, shared outbound terminal-failure, delivered-status, and delivery-count query helpers, dashboard next-step/outbound-delivery rate/pending/failure signals, campaign list recipient readiness plus list/detail outbound delivered/pending/failed delivery reporting and campaign-detail row-state classification, and analytics mutually exclusive outbound-delivery breakdown coverage, Playwright smoke/demo/product-demo, contract/secret/compliance/production gates including Clerk auth config blocking, production auth/RBAC planning check, production worker policy check, and current handoff context-budget check. | Browser smoke and seeded product demo paths cover main local workflows, including visible product composer preflight counts and requested inbox thread selection. | Production readiness remains intentionally gated. | Keep local gate green; prefer high-value correctness/product work over more syntactic auth variants. |
| Docs/Planning | Canonical plan, contracts, local gate docs, compact current handoffs, `npm run agent:brief`, append-only historical logs, and context-budget validation. | N/A | Handoffs must stay short enough to guide new agents without reloading old runs. | Start loops with `npm run agent:brief`; keep `PLAN.md`, this matrix, and `docs/NEXT_PROMPTS.md` concise; preserve history in loop logs. |
