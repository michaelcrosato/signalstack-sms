# Merge Playbook

Milestone 0 merge rule: merge only after `npm run validate` passes or an exact local environment blocker is documented.

Future integration merges should use rollback tags and preserve contract-first updates.
