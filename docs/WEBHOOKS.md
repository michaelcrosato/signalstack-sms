# Webhooks

Milestone 0 has no live webhook behavior. Future Twilio handlers must validate signatures, store raw event data, enforce idempotency, and never send replies unless live messaging gates pass.
