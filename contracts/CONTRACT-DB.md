# Database Contract

Owner: backend-data.

Milestone 0 defines a minimal Prisma schema for validation only. Future tenant-scoped models must include `orgId` unless explicitly documented here.

Baseline models: `Organization`, `AppUser`, `Membership`, `Contact`, `Campaign`, `Conversation`, `Message`.
