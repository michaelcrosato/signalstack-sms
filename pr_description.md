🎯 **What:** Added missing unit tests for the `liveAiIsBlocked` function in `lib/ai/ai-gate.ts`.
📊 **Coverage:** Covered the happy path (returning `false` when all live AI conditions are met) and error conditions (returning `true` if API key is missing or AI provider is not 'live').
✨ **Result:** Improved test coverage for the AI gate functionality, ensuring the logic backing the live AI enablement is robust.
